# E1 笔头、沾色与局部绘制过程

用户已确认新一轮三项增量，基线 a4ea620，分支 codex/e1-image-painting。不上线、不合并 main。旧三张照片、构图、seed 1906 与历史证据保留。M1 V4 / M2 / E1 人工验收继续待用户确认。

## A：实际笔头（已验证）

显示读取 Painting 最近一次真实接触的宽度、方向、位置、颜色和上色量；按宽/中/细扁刷呈现，同一扁刷族，没有伪造新圆头笔。笔头含刷毛和金属/木柄，落笔展开、抬笔收拢；所有显示都独立于作品与 PNG。

- 类型检查、构建：通过。
- E1 引擎 6 项：通过，包括只读接触、手动及历史计划精确一致、各速度与笔内暂停。
- 浏览器笔内暂停/导出/降级/继续及 M2 隔离：通过。最终颜色/高度仍等于此前固定风景完整结果。
- 证据：artifacts/e1/brush-process/a-engine、a-browser/pen，含真实录像、暂停页面、导出 PNG。
- 首次调用 npm 传额外参数未正确限定项目，手动中断后用显式 CLI 重跑；中断运行没有完整报告，不作为通过证据。

## B：沾色与局部顺序（已验证）

规划器 e1-brush-process-3 / processVersion 1 在确定计划中记录每次取色的位置、颜色、笔宽、上色量；刷毛执行版本仍是 2。播放使用完成取色后的同一参数进入 Painting，取色期间不绘画。暂停冻结当前动作，重播清空实验状态并从首次取色开始。

顺序只在 128×128 局部、最多前看 24 笔内调整，保守包围盒必须与被跨过的全部笔触不相交；同时检查整阶段画布内移动路程与颜色切换综合代价未增。所有路径、颜色、大小、上色量、种子及覆盖关系保留。sourceOrder 记录原先顺序，供核对。它不识别物体，也不声称模拟画师思考。

- E1 引擎 8 项：通过。原三张图新顺序的颜色/高度分别与上一版精确相同，.5/1/4× 和暂停的最终状态相同。历史手动/混色及旧计划回归通过。
- 沾色浏览器：通过，含真实选图、首次沾色暂停、接触参数对照、实际 PNG、隐藏笔头/沾色台/参考后 PNG 不变、换图取消、退出保留 M2 草稿。
- 类型检查与构建：通过。
- 证据：b-engine-verified/ordering、b-browser/dipping。
- 第一遍逐笔断言产生过多测试框架记录，完整三图比较用时 2.7 分钟；中断后把同样的逐笔参数数组序列化整体比较，重新执行全部 8 项用时 41.2 秒。未改引擎或放宽检查，旧 ordering 记录保留。

当前沾色是明确加载目标色，不模拟余色、颜料耗尽或调色配方。为维持现有成品逐字节不变，不把相近颜色强制合并；因此仍会频繁往返沾色台。画布内相邻笔之间的路程减少，不代表包含新增沾色动作的总路程或总时长减少。

C 全程三图录像、浏览器完整回归与连续性能：尚未完成，继续执行。
