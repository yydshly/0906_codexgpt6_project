# E1 笔头、沾色与局部绘制过程交付

用户确认的三项已开发并验证：笔头显示对应真实接触；沾色参数用于实际落笔；局部不相交笔触按距离和相近颜色调序。基线 a4ea620，继续 codex/e1-image-painting，不合并 main、不部署。M1 V4、M2 及 E1 人工画作/手感/过程验收仍为 **待用户确认**。

本轮成品颜色、高度、五个阶段状态和实际 PNG 均与上一版相同。改善的是过程，不把笔头外观或调序的数值指标当作画质提升。没有增加风格、圆头笔、余色混合、颜料耗尽、AI 服务或画师教学。

## 实际入口与复现

- [打开慢光画室](http://127.0.0.1:5174/)：右上角「图片自动绘制 · 实验」。
- [打开笔头、三图与完整录像验收页](http://127.0.0.1:5174/artifacts/e1/brush-process/index.html)：可回到画室，也关联 M1/M2 和上一轮 E1 历史资料。
- 在项目目录运行 `npm run dev`。本轮复用了现有服务，HTTP 200 已验证；启动日志引用 `artifacts/m2/server.log`，没有将旧启动日志说成本轮新启动。记录见 `checks/startup.json`。

验收步骤：选固定照片 → 保持「完整保留」→ 确认绘制 → 用 0.5× 观察沾色与笔头 → 落笔中暂停并导出 → 继续/4× 完成 → 重播 → 返回原画室核对草稿。实验不会自动保存，请退出前导出。上色量显示是绘画参数，不是颜料物理体积。

## 实施增量与文件

A（9b88381，已推送）：`Painting.contact` 只读提供最近一次实际接触的位置、宽度、方向、颜色及上色量。`BrushCursor.tsx` 绘制木柄、金属箍、刷毛，宽/中/细扁刷跟随真实笔宽；落笔展开、抬笔收拢。刷毛外观是矢量示意，没有逐根刷毛物理模拟。引擎落笔、混色、撤销计算不变。

B（11b4967，已推送）：`process-plan.ts` 在每个 128×128 局部最多前看 24 笔，只允许跨过保守包围盒完全不相交的笔触，并检查整个阶段的画布内移动路程和颜色切换综合代价不增。`sourceOrder` 保留原顺序。路径、颜色、大小、上色量、模式、seed 和重叠先后全部保留。

计划使用 `plannerVersion=e1-brush-process-3`、`processVersion=1`，刷毛执行版本仍为 2。`pickups` 记录执行前序号、颜色、笔宽、上色量。播放器依次执行备色、移动到沾色台、沾取、移到画布、真实落笔与抬笔；完成取色后的同一参数传给 Painting。取色/移动不写作品，暂停冻结当前动作，重播清空独立实验状态并重置取色。旧计划无需取色字段仍可精确执行。

C：固定证据、全程统计、回归和验收导航。播放统计保留最多前 20000 个批次/帧间隔，额外记录全程批次数、最大批次和连续超限次数。临时测试输出隔离到各证据目录的 `.runtime`。SOURCES.md 记录原创矢量与调序实现，无新增依赖或第三方图片。

## 测试结果

本节路径均以 `artifacts/e1/brush-process/` 为根。

| 项目 | 状态 | 本轮证据与范围 |
| --- | --- | --- |
| 类型检查、构建 | 通过 | `checks/final-typecheck.txt`、`final-build.txt` |
| M1 引擎 7 项 | 通过 | `c-engine/tests-results.json`；独立快照、20/21 笔逐次颜色/高度撤销、故意错误高度恢复反向验证保留 |
| E1 引擎 8 项 | 通过 | 同上；只读接触、旧计划/手动画笔不变、固定三图调序精确一致、取色校验及 .5/1/4× 包含混色与暂停 |
| M1 浏览器 4 项 | 通过 | `regression/tests-results.json`；固定笔触、UI SHA-256、实际 PNG、输入与 WebGL 降级/恢复 |
| M2 浏览器 8 项 | 通过 | 同上；引导、草稿准确恢复、恢复后新笔撤销、失败/损坏/多标签页保护、签名/预览/真实 PNG |
| E1 浏览器全套首跑 10 项 | 9 通过 / 1 未通过 | `c/tests-results.json`；唯一失败为静物录像落盘，功能断言已通过，详情见下文 |
| 同一静物完整补录 | 通过 | `c-recording-retry/tests-results.json`；相同输入、参数、逐阶段对照与真实录像重新执行 |
| 最终文案/导航后沾色浏览器复测 | 通过 | `final-dipping/tests-results.json`；暂停、真实接触、PNG 无覆盖层、取消和 M2 隔离 |
| 独立连续性能 | 通过（有孤立帧间隔） | `performance/continuous.json`，完整固定复杂图，4×，无录屏、截图、导出和并行测试 |
| 验收媒体/入口实操 | 通过 | `review/check.json`：26 张图片解码、48 个链接 HTTP 成功、5 段视频前/中/后实际解码；画室新开验收页、选图、暂停、真实下载再验证 |
| M1 V4 / M2 / E1 人工验收 | 待用户确认 | 不因自动化通过而改变 |

浏览器全套验证包括首次取色暂停不写数组，笔头位置/方向/宽度读取实际接触；颜色和上色量来自完成的取色；隐藏笔头、沾色台及参考后导出字节不变；笔内导出和图形上下文丢失不结束当前笔；不同图片有不同结果；相同计划完整 4× 与 1× 最终一致；取消、换图、退出不破坏 M2 画作/签名/引导/撤销/存储。历史 UI SHA-256 与逐笔状态断言均保留。

复现时显式设置新的输出目录，避免覆盖历史：

```powershell
$env:M1_ARTIFACT_DIR = 'artifacts/e1/brush-process/local'
npm run typecheck
npm run build
node node_modules/@playwright/test/cli.js test --project=engine --project=e1-engine
node node_modules/@playwright/test/cli.js test --project=studio --project=m2
$env:E1_EVIDENCE_SPEED = '4'
node node_modules/@playwright/test/cli.js test --project=e1
node node_modules/@playwright/test/cli.js test --project=e1-performance
```

加入取色后，完整等待为 1200 秒，包含两次完整播放的用例总时限为 1800 秒；它们只是功能等待，不是性能通过标准。Worker 120 秒上限、批次 p95 ≤50 ms、连续三个 >100 ms 的停止门槛不变。正常速度观看数千次取色本来就更久，4× 是调度倍率，录屏/窗口调度仍会影响墙钟时间。

## 固定三图、录像与性能

原照片与许可继续引用 SOURCES.md，输入 SHA-256、构图、seed 1906、512 分析及 1024 原生落笔固定在 `manifest.json`。三张图没有换样本，全部展示。静物最终证据采用 `c-recording-retry/still-life`，另外两张在 `c/landscape`、`c/complex`，各含原图相对入口、参数、计划、五阶段 PNG、实际导出 PNG、页面、录像和原始结果。

| 样本 | 笔数 / 取色次数 | 规划 | 到最后阶段（含取证暂停） | 批次 p95 / 最大 | 画布内笔间路程减少 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 风景 | 6735 / 6599 | 5.93 s | 229.32 s | 6.30 / 13.80 ms | 9.8% |
| 静物（补录） | 7187 / 7110 | 5.50 s | 386.01 s | 6.10 / 17.60 ms | 14.4% |
| 复杂街景 | 7036 / 6919 | 4.81 s | 210.04 s | 6.00 / 8.60 ms | 12.3% |

画布内路程不包含新增往返沾色台，不能说总路程或总时间减少。没有把相近色强制合并，保留了原成品，也因此取色非常频繁；长程过程仍可能显得机械。录像期间部分运行并行了独立功能/引擎检查，采集与窗口调度会增加时长；该表是诊断数据，独立性能另测如下。

独立复杂图 4×：全程（含规划）203.83 秒，规划 5.01 秒；10971 个批次全部纳入统计，p95 3.60 ms、最大 20.70 ms，无 >100 ms 批次。播放期 rAF 间隔 p95 8.40 ms，最大 433.20 ms，出现 1 次 >100 ms 间隔，未记录到对应 JS 长任务，原因未确认。没有持续批次超限，但不能宣称零卡顿，也不能把 rAF 当输入延迟。增加动画等待会改变批次分布，不能用更低 p95 声称刷毛计算加速。

环境：Windows 11、Node 22.15、Chrome 152，1440×900、DPR 约 1，ANGLE Intel UHD Graphics / D3D11 / WebGL2。实际展示背板约 401×401，作品颜色/高度及导出仍为原生 1024×1024，未把显示分辨率当作品分辨率。

五次载图/退出，页面 heap usedSize 从 6,084,628 到 8,455,992 字节，增长 2.26 MiB，低于原 <24 MiB 检查。自动画作颜色/高度 6 MiB、复用混色缓冲 4 MiB、撤销 0；手动最近 20 笔仍为 120 MiB 历史上限。页面堆测量不包括 GPU、Worker 和全部浏览器原生缓冲，未声称整机峰值通过。

5 段入口录像时长依次为 11.60 s, 245.52 s, 418.88 s, 223.32 s, 1033.36 s：慢速沾色、风景、静物补录、复杂街景、完整 4×/1× 重播与 M2 隔离。高速下短笔和部分取色可能落在同一录屏帧内，0.5× 更便于观察。所有视频是实际浏览器操作，不是揭幕或成品视频播放功能。

## 失败与修正记录

1. A 首次 npm 附加参数未限定项目，手动中断后用显式 CLI 重跑；无完整报告的中断不算通过。
2. B 初次大量逐笔断言带来测试框架记录负担，三图比较用时 2.7 分钟；中断剩余运行后，把同样的全部逐笔参数序列化整体比较，8 项完整重跑通过。未改引擎或降低断言范围；`b-engine/ordering` 保留。
3. C 静物首次功能/PNG 对照通过，但 `video.saveAs` 报 ENOENT：并行引擎测试清理了共享 test-results。整条取证未通过。已隔离每组 `.runtime`，同一图片/参数完整重录通过，未换图。`checks/still-life-recording-error.md`、`validation-overrides.json` 和原 `c/tests-results.json` 保留失败；原 `c/still-life/results.json` 是落盘前功能记录，不能单独代表整条通过。
4. 最后仅把沾色台标签改成「当前备色」以区分已在笔上的颜色，并连接新验收页；重复类型/构建、沾色回归、独立性能及入口下载烟测。三图视频仍是相同绘画/播放器逻辑，不为标签变化覆盖历史录像。

## 已知限制、引用记录与停止点

- 主要构图保持现有水平，复杂文字、人脸、车和细枝等精确还原仍未通过；本轮没有改进它们。画师语义理解、真实颜料余色/耗尽、多风格、圆头笔、实验恢复、中途接手：未实现。
- M1 V4 与 M2/E1 真人画作、手感和长期观看体验：待用户确认。真机触控/压感、低配置设备、其他浏览器、最大输入整机压力/长期峰值：未测试。
- 本轮阶段页面已检查标题与作品，未重现上一轮截图中的标题空白；没有逐帧确认所有长录像，不能宣称该历史偶发问题彻底解决。`review/studio.png` 是滚动到导航后的视口，标题在视口外；首屏与真实接触见 `final-dipping/dipping/real-contact.png`。
- 原 M1 全套热路径性能、M2 60 秒连续绘画/保存仍引用上一轮 E1_REFINEMENT_REPORT.md 对应历史记录，本轮没有重跑，也没有改绘画热路径或草稿代码。本轮新测 E1 全程性能与资源释放；M1/M2 功能及降级已经重跑。
- 三图最终 PNG 与上一版逐字节一致，`review/check.json` 留证；量化结果用于一致性/调度诊断，不替代画作质量判断。
- 既有 M1/M2/E1 历史文件未修改，私人图片/结果未进入仓库，工作流与线上 M2 未变。A、B 已分别提交推送，本报告、最终证据和回归随 C 提交推送 E1 分支。

本轮完成后停止，等待画作与过程人工验收，不部署、不进入图片语义或多风格扩展。
