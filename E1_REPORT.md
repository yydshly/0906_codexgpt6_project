# E1 图片自动绘制实验

E1-A/B/C 已形成当前应用内可运行的实验。只在实验分支提交推送；不合并 main、不触发部署、不覆盖线上 M2。开发验证与人工画作验收分开，后者仍待用户确认。

本地启动：`npm install`（已有依赖时不需要）后 `npm run dev`。本次沿用已运行的 Vite 进程，启动日志与 HTTP 200 确认地址为 http://127.0.0.1:5174/，日志摘录在 artifacts/e1/startup.log。打开画室，点击「图片自动绘制 · 实验」。资料入口为 http://127.0.0.1:5174/artifacts/e1/index.html；也可直接打开仓库内该 HTML。这些本地地址需要开发服务保持运行。

基线：2026-09-06 核对 main / origin/main 为 c5a56a4，工作区干净；从此创建 codex/e1-image-painting。线上 M2 保持原样。M1 V4、M2 完整体验和 E1 画作/过程人工验收均待用户确认。

## 增量 A：实际单图重绘

已在慢光页面增加独立实验入口，保留原画室。照片只经本地解码、256×256 构图分析；Worker 使用现有 Painting 计算多尺度差异并生成有序计划。主线程按计划调用同一 Painting，StudioRenderer 以原材质路线显示和导出原生 1024×1024 PNG。没有图片写入作品数组或揭幕动画。

固定三张公开照片及 SHA-256、输入参数见 artifacts/e1/fixtures/manifest.json，许可和原图来源见 SOURCES.md。黄石输入为 Commons 的完整构图 1280 缩略版，另外两张为原文件。未使用用户私图。A 尚未做效果修正，保留所有首次输出。

自动实例关闭撤销历史，覆盖不复制整图，混色复用一个 4 MiB 落笔前颜色缓冲。手动默认实例仍逐笔保存最近 20 笔颜色/高度历史。新增 24 笔混色/覆盖逐笔对比测试证明两种实例输出字节一致。

| 项目 | 状态 | 记录 |
| --- | --- | --- |
| typecheck / build | 通过 | 首次 A 构建，未增加依赖 |
| 原 test:engine | 通过 | 7 项，独立预期和错误高度反向检查保留 |
| E1 引擎/输入头/页面闭环 | 通过 | 3 项；原画室颜色/高度不变 |
| 冬景原生 PNG / 实际录像 | 通过 | artifacts/e1/a/landscape.png、actual-process.webm |
| 冬景自动历史占用 | 通过 | 0；颜色/高度 6 MiB，复用混色缓冲 4 MiB |
| A 本机响应性诊断 | 通过 | 3463 笔；规划 5795.8 ms；播放批次 p95 18.1 ms / max 30.1 ms；单笔 max 30 ms；调度间隔 p95 29.3 ms（不是输入延迟） |
| M1/M2 完整浏览器回归 | A 时未测试；C 已通过 | 见下方最终验证 |
| 三图阶段证据、速度/重播/取消验证 | A 时未测试；B/C 已通过 | 见下方最终验证 |
| 人工画作/过程质量 | 待用户确认 | A 冬景大构图可辨；小枝、栅栏有损失，刷痕明显，不能宣称参考效果通过 |

环境：Windows 本机 Chrome / ANGLE Intel UHD / WebGL2，DPR 1，实际实验画布显示约 501×501，导出 1024。原始数值在 artifacts/e1/a/metrics.json；A 截图、计划、PNG、录像与测试 JSON 同目录。构建包含既有验收导航与 47 份历史证据，不发布 E1。

## 约束与停止条件

输入最多 12 MiB / 1200 万像素 / 单边 8192；规划固定 256 分析尺寸、80/40/20/10 笔宽，总计最多 3770 笔、每笔最多 7 个路径点。seed 1906、画笔版本 1、画布 seed 906。固定后不换图；至多两轮聚焦修正，若大构图仍不能保持或持续卡顿则停止扩大功能。预先记录的本机试验门槛为播放批次 p95 ≤ 50 ms，无连续 >100 ms 批次。数值只作诊断，不代替人工判断。

实验不持久化、不上传，不接模型/付费服务；不做接手、跳转、视频导出功能。录像仅为实际浏览器测试证据。

## 增量 B：构图、播放控制与结果保护

完整保留/居中方形两种构图在分析层预览，明确裁切范围，不改变既有结果；选图、重规划或重播替换已有实验时有取消/先导出/确认选择。暂停在当前整笔结束生效，速度 0.5/1/4 只改变批次调度，重播不重新规划。暂停、导出与退出互相协调，未确认退出时结果保留。任务采用代次校验与 Worker 终止；规划 120 秒限时。模态确认时背景不可操作。

B 类型检查和构建通过。实际浏览器 E1-B 测试通过（1.3 分钟）：暂停后颜色/高度与笔数冻结；同一静物计划以 4× 含暂停和 1× 重播，最终两数组 SHA-256 完全相同；实际下载与 renderer PNG 相同，移除参考图后 PNG 字节仍相同；取消重播/更换/退出保留实验结果；退出后 M2 颜色、高度、IndexedDB 记录、签名、引导步骤和当前撤销数量均未变。证据在 artifacts/e1/b，含静物首次 PNG、完整控制录像、输入计划、独立哈希和截图。

本轮未修改规划算法/固定参数，效果修正轮数仍为 0。C 已补齐三张固定图的逐阶段证据和完整异常/历史回归；B 的自动化结果不代表人工效果通过。

## 最终 C 验证与证据

| 项目 | 状态 | 实际执行与证据 |
| --- | --- | --- |
| 类型检查 / 生产构建 | 通过 | artifacts/e1/regression/typecheck.log、build.log；未增加依赖，构建含本地 Worker |
| 原 M1 引擎 | 通过 | test:engine，7 项；逐笔颜色/高度独立预期、错误高度反向检查均保留 |
| 原 M1 浏览器 | 通过 | test:e2e，4 项；固定笔触、混色、UI SHA-256、实际 PNG、20 笔撤销和降级回归保留 |
| 原 M2 浏览器 | 通过 | test:m2，8 项；引导、草稿事务/恢复/错误/冲突、签名预览导出与返回修改 |
| E1 自动实例与手动实例等价 | 通过 | E1 engine 2 项，24 笔覆盖/混色逐笔颜色和高度完全一致；输入头异常拒绝 |
| E1 首次页面闭环和完整控制 | 通过 | A、B 共 2 项；原生真实 PNG、计划、录像与独立哈希 |
| 三种固定输入的真实重绘 | 通过 | C 3 项；每张独立输入/计划/四阶段 PNG/最终导出/真实录像/指标，三个结果与计划各不相同 |
| 结果实际来自笔触 | 通过 | Node 中从空白只执行序列化计划、完全不读取原图，三图最终颜色和高度 SHA-256 与页面一致 |
| 同图可复现规划 / 同计划重播 | 通过 | 同图两次规划完整 JSON 相同；B 的含暂停 4× 与 1× 重播两数组一致；现有画笔内部只使用计划给定 seed 的固定刷毛分布，不使用帧时间或非确定随机源 |
| PNG 不夹带参考 | 通过 | 隐藏/移除参考层后 PNG 字节一致；实际下载与材质导出一致，原生 1024×1024 |
| 无效图片 / 大文件 / 超大尺寸 / 解码失败 | 通过 | c/errors，错误有提示；无效替换保留当前实验结果 |
| 取消规划 / 更换 / 退出 | 通过 | 旧 Worker 终止、旧播放器停止；延迟 800ms 的旧解码不能覆盖新选图；M2 数组和已存记录未改 |
| Worker 不可用 / 超时 | 通过 | final-safety：注入构造失败；将配置的 120 秒限时在测试中加速到 50ms 验证超时路径，不冒充真实等待 120 秒 |
| WebGL 丢失与资源释放 | 通过 | 实验转 Canvas 2D，颜色/高度不变且可导出；退出后主动释放实验 WebGL 上下文，5 次进出未使主画室降级 |
| 连续播放性能补充 | 通过 | test:e1perf，固定复杂图不录像/截图/编码；批次 p95 13ms / max 33.3ms，无 >100ms 批次 |
| 本地资料入口与媒体 | 通过 | e1-review-check：18 张图片、全部资料链接和 3 段录像可打开/播放；390px 模拟视口没有实验横向溢出 |
| M1 V4 / M2 人工完整体验 / E1 画作与过程 | 待用户确认 | 没有自动升级为通过 |
| 真机触屏、压感笔、低性能设备、多浏览器及长时间使用 | 未测试 | 当前证据来自 Windows 桌面 Chrome；视口检查不代表真机；Canvas 2D 完整自动播放性能与特殊图片编码未全面测试 |

E1 共 10 项功能/引擎用例在 A、B、C、final-safety 分组实际执行，另有 1 项连续播放性能测试和本地资料检查。原始 Playwright JSON 留在各组目录，不覆盖早先记录。旧测试的新结果与固定样本归档在 artifacts/e1/regression，没有覆盖 artifacts/m1 或 artifacts/m2。原 M1/M2 的完整性能基准未重复；其历史记录仅作为历史资料，不充作 E1 性能结论。

### 三张固定样本

| 样本 | 笔数 | 本次规划 | 播放批次 p95 | 已知差异 |
| --- | --- | --- | --- | --- |
| 黄石冬景 | 3463 | 7.50 s | 17.1 ms | 大构图对应原图；细枝/栅栏弱化，雪地与留白交叉刷痕偏重 |
| 咖啡杯静物 | 3435 | 5.35 s | 17.4 ms | 杯口/杯碟/豆粒布局保留，杯把孔洞与豆粒轮廓模糊，白瓷纹理过强 |
| 夜间街景 | 3527 | 5.70 s | 17.3 ms | 建筑/灯光/街道色带对应；文字不可读，人物/车辆/斑马线细节明显丢失 |

每张的全部交付在 artifacts/e1/c/{landscape,still-life,complex}/：input.json、plan.json、stage-1..4.png、final.png、process.webm、results.json、differences.md 及页面截图。原图在 fixtures；公开素材出处与许可在 SOURCES.md。录像记录真实自动化浏览器操作，阶段末暂停采集证据，不是预先制作的成品动画。控制录像单独在 b，异常录像在 c/errors，补充状态释放录像在 final-safety/lifecycle。

固定计划、颜色参数、轨迹生成规则和 seed 均未改，效果聚焦修正为 0 轮。A 与 C 的冬景完整计划 JSON 字节相同。三个输入/计划/结果指纹见 c/comparison.json。大构图观察与失真都同时展示，没有只选最好结果；不承诺高质量复刻，也不将粗细阶段宣传为画师步骤。

### 性能与内存范围

上述三图批次 p95 均满足预先固定的 ≤50ms 门槛，没有连续三个 >100ms 批次。三张图单笔最慢分别约 31.3 / 27.5 / 49.6ms。规划放在独立 Worker，不在播放时重新规划。播放器每批预算 6ms，但不可拆分的当前整笔可超过预算，因此暂停在整笔结束后生效；速度改变笔数/调度间隔，不改变序列或画作。

阶段录像采集期间也记录了 51–139ms 的主线程长任务，调度间隔单次最高 475ms。不能仅凭批次 p95 宣称“毫无卡顿”；它们包含证据采集环境的影响，原因未逐个归因。补充的无录像/无截图/无导出连续播放记录另存 performance/continuous.json：批次 p95 13ms，规划期间 rAF 间隔 p95 4.3ms / max 45.9ms，播放期间 rAF 间隔 p95 8.4ms / max 33.2ms，均无 >100ms 间隔。帧间隔不是输入延迟，未宣称触笔延迟或人工手感通过。

每个自动实例颜色/高度 6MiB、复用混色缓冲 4MiB、撤销历史 0。覆盖模式不复制落笔前全图，混色每笔向复用缓冲复制颜色以保持原语义；没有为几千笔创建全图历史。原手动实例仍保留最近 20 笔、上限 120MiB 快照。Renderer 另有 4MiB CPU 缓冲和 6MiB GPU 纹理；规划 Worker 自有一份自动 Painting，完成或取消立即终止。

输入文件最多 12MiB，解码前验证最大 1200 万像素（RGBA 约 45.8MiB）、单边 8192；分析数组为 256×256，计划最多 3770 笔，每笔最多 7 个点，统计数组最多 20000 个元素。PNG 编码复用既有短生命周期 Worker。CDP heap 数据与五次载图/退出后的 GC 记录见 final-safety/lifecycle/results.json：只能说明页面 isolate 与 backing storage 的变化，不是整个浏览器/GPU/Worker 的实测峰值。整机长期峰值内存、最大输入压力和低内存设备尚未测试。

### 人工验收步骤

1. 打开慢光，保留或恢复一份 M2 画作，再点「图片自动绘制 · 实验」。原画室没有被清空。
2. 选一张本地 PNG/JPEG，比较「完整保留」和「居中方形」预览；检查裁切提示，确认后开始绘制。
3. 观察空白到大色块、轮廓与细节的实际过程，暂停后确认不再新增笔触，继续并切换速度。
4. 尝试重新播放，先取消确认验证原实验仍在，再确认重播；对照原图判断构图、色彩和刷痕是否可接受。
5. 导出当前 PNG，检查 1024×1024、无原图/工具/鼠标，画面与当前结果对应。
6. 尝试更换图片、无效图片、取消和退出提示；确认退出后检查原 M2 画作、签名、引导与撤销仍在。
7. 打开本地证据页逐张查看原图、阶段、最终图和录像，分别确认可接受的题材与失真边界。E1 不自动持久化，退出或刷新前先导出。

实现集中于 src/experiment 的输入、规划 Worker、计划播放器和独立界面；src/main.tsx 仅增加入口和挂载状态；src/painting/engine.ts 仅增加自动实例的历史开关与复用颜色缓冲。草稿、签名、材质 shader、M1/M2 测试及固定历史样本没有重写。资料生成/链接媒体检查脚本在 scripts/e1-evidence-index.mjs、e1-review-check.mjs。

可复验命令：`npm run typecheck`、`npm run build`；设 `M1_ARTIFACT_DIR` 为新的证据目录后运行 `npm run test:engine`、`npm run test:e2e`、`npm run test:m2`、`npm run test:e1`、`npm run test:e1perf`，以免覆盖历史输出。本次所有日志和实际 JSON 为依据，不以仅列出命令代替执行。

提交顺序：78c2adb（E1-A 真实重绘）、ea88a1a（E1-B 构图控制），E1-C 包含完整证据、异常/性能回归和此报告。均推送 codex/e1-image-painting，未修改部署工作流或推送 main。不进入接手、任意时间跳转、视频导出功能、模型接口或下一阶段。等待画作与过程验收。
