# 仅本地的绘画诊断工具

本工具用于隔离检查分析图、色盘映射、真实颜色和材质结果，不改变应用中的原版或结构优先模式。用户提供的图片、派生图片、计划、截图、输入坐标和诊断数据必须保存在仓库以外，不能提交或放到开发服务目录。

`scripts/local-painting-diagnosis.mjs` 接收本地 PNG、仓库外输出目录、可选的源图像素矩形，以及固定试验参数。它在独立 Chrome 中读取本地应用模块，通过同一 Painting/StudioRenderer 执行真实笔触。外部页面请求被阻止；复制输入、生成 PNG/计划/HTML 全部在仓库之外。脚本拒绝输出到仓库内，并检查真实路径避免链接目录绕过。

可用的诊断条件：

- 默认 24 色：复现当前原版和结构优先路径。
- `96`：仅在该独立浏览器响应中替换色盘数量，不修改应用文件。每份计划注明诊断版本。
- `96 focus`：在上一步基础上，对调用者手动给出的矩形做有界补笔。最多 4000 分析网格、两次扫描，笔宽 4、覆盖模式。这不是自动人脸识别，也不是已验证的生产规划器；可能超过应用现有整幅预算。

调用格式：

```text
node scripts/local-painting-diagnosis.mjs INPUT_PNG OUTSIDE_REPO_DIR [x,y,width,height] [24|96] [off|focus]
node scripts/local-painting-review.mjs BASELINE_DIR PALETTE_DIR FOCUS_DIR
```

第二个工具先只使用序列化计划独立执行并核对颜色、高度，再在私人目录生成同范围局部对照。对照中的色盘映射明确为诊断图，不是笔触画作。补笔实际结果不会夹带原图。

本轮工具检查：语法检查通过；仓库内输出被拒绝；本地诊断执行通过；两种路径的补笔计划独立重放状态一致；图像对照可打开。私人诊断的具体结果、图像和数据没有提交。

限制：这是离线问题定位，不是实时过程录像、性能验收或自动画质判定。应用源码未修改，未重复无关的 M1/M2 构建及全套回归；之前记录仍是之前的测试结果。工具用项目既有 Playwright 和本项目原创诊断代码，无新依赖、素材或收费服务。只同步通用工具和本文，不合并 main 或部署。
