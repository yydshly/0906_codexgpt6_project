# E1 可选结构优先模式

日期：2026-09-06。分支：`codex/e1-image-painting`。原能力基线：`4c18e99`。首个可运行增量 `056a723` 已检查并推送。只增加独立可选方式，不替换原版、合并 main 或部署。

## 使用

本机开发服务：`npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。本轮浏览器实际使用 `http://127.0.0.1:5174/`，复用已有服务。

进入“图片自动绘制 · 实验”，默认选中“原版 · 油画笔触”。如需试新方式，点击“结构优先 · 实验”，选图和确认构图，再准备笔与颜色、确认开始。两种方式均实际绘制，支持暂停、继续、重新播放、原图对照和 PNG 导出。

切换选项会暂停当前绘制并保留作品。准备替换前仍提供取消、导出和确认；取消不改作品数组。切回当前计划对应的方式后可以继续原计划。页面同时显示所选方式与当前画作方式，避免把尚未重新生成的画作当成新模式结果。

## 改动及边界

- `ImageExperiment.tsx` / `prepared.css`：在现有图片实验中增加两个方式按钮及状态说明，保留主画室和原页面材料布局。
- `planner.worker.ts` / `plan.ts`：Worker 接收可选方式；缺省仍执行原路线。新计划版本 `e1-structure-1`，原版仍为 `e1-prepared-studio-2`。保留 512 分析、1024 实际落笔、seed 1906、brushVersion 2、五阶段和原笔数上限。
- `structure.ts`：局部颜色边缘权重；按阶段、64px 分析网格区域、实际笔宽及色盘颜色组织连续任务。任务在落笔前安排，每笔仍用真实 Painting 状态复查颜色误差。随后沿用不相交笔触的有限排序。不跨阶段全局集中颜色。
- `materials.ts`：新模式的 24 色上限内加入边缘权重，原版权重保持原计算。最多 8 种实际宽度；笔具和色盘仍对应执行参数。
- 新模式末两阶段使用未额外模糊的分析色，固定上色量 0.9，细化厚度 0.065，保留扁刷刷毛及真实颜色/高度计算。手动引擎和渲染器没有改动。

这不是人物识别、专业画师步骤、颜料耗尽模拟或照片级精修。任务区域是几何网格。新增模式改变计划、色盘和实际作品，不保证与原版状态相同；原版计划和历史执行结果必须保持一致。没有引入模型、接口、依赖、第三方代码或新素材；原三张图许可仍见 SOURCES.md。私人截图、私人图片及其结果均未纳入公开证据。

## 验证记录

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 类型检查、构建 | 通过 | 首增量实际执行；最终回归另补记录 |
| 局部任务不丢候选、边缘权重和透明区域 | 通过 | `a-checks/npx-results.json` |
| 新模式确定性、上限、测试时钟三速度和暂停 | 通过 | 同上，合成双区域只用于功能验证，不作画质样本 |
| 原版完整风景计划与历史逐字段相同 | 通过 | `a-checks/switching/results.json` 及对应测试 |
| 模式切换/取消保留、切回继续、新模式实际落笔导出、M2 草稿隔离 | 通过 | `a-checks/switching/process.webm` 与状态记录 |
| 原三张完整实际绘制及独立颜色/高度重放 | 通过 | `b-fixed/npx-results.json`，三张各五阶段 PNG、4× 实际录像、导出无参考层与独立执行状态一致 |
| 完整新计划全部三速度 | 未测试 | 待固定新计划记录后执行 |
| M1/M2 与既有 E1 回归 | 未测试 | 待执行 |
| 独立连续性能、内存与降级 | 未测试 | 待执行 |
| 人物原图精度、真机触控、真实用户体验 | 未测试 | 未获得对应私人原图/计划，不新增人物样本冒充原问题已解决 |
| M1 V4、M2 人工验收、新模式画作与过程 | 待用户确认 | 自动测试不替代人工验收 |

证据独立存于 `artifacts/e1/structure-mode`，不覆盖任何旧图片、固定参数、视频或报告。

## 已知限制与停止点

结构优先只是可选实验，不是已证明全面优于原版。局部批次有望减少取用，但会改变覆盖顺序和颜色分布，必须展示同图对照；减少动作不代表人物相似度提升。全局有限色盘、512 分析及最小 4px 扁刷仍限制小五官、细字和复杂边界。颜色边缘也可能把背景细碎纹理排在重要语义之前。

本轮不实现模型识别、多风格商店、图片持久化或中途手动接管。保持独立实验退出前导出提示。性能门槛沿用批次 p95 ≤50ms、不得连续三批 >100ms；录屏指标与独立测量分开。当前工作范围内最多两轮聚焦修正，不能换测试图片掩盖问题。
