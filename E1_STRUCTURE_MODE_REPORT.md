# E1 可选结构优先模式

日期：2026-09-06。分支：`codex/e1-image-painting`。原能力基线：`4c18e99`。首个可运行增量 `056a723` 已检查并推送。只增加独立可选方式，不替换原版、合并 main 或部署。

## 使用

本机开发服务：`npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。本轮浏览器实际使用 `http://127.0.0.1:5174/`，复用已有服务。

进入“图片自动绘制 · 实验”，默认选中“原版 · 油画笔触”。如需试新方式，点击“结构优先 · 实验”，选图和确认构图，再准备笔与颜色、确认开始。两种方式均实际绘制，支持暂停、继续、重新播放、原图对照和 PNG 导出。

切换选项会暂停当前绘制并保留作品。准备替换前仍提供取消、导出和确认；取消不改作品数组。切回当前计划对应的方式后可以继续原计划。页面同时显示所选方式与当前画作方式，避免把尚未重新生成的画作当成新模式结果。

## 改动及边界

- `ImageExperiment.tsx` / `prepared.css`：在现有图片实验中增加两个方式按钮及状态说明，保留主画室和原页面材料布局。
- `planner.worker.ts` / `plan.ts`：Worker 接收可选方式；缺省仍执行原路线。新计划版本 `e1-structure-1`，原版仍为 `e1-prepared-studio-2`。保留 512 分析、1024 实际落笔、seed 1906、brushVersion 2、五阶段和原笔数上限。
- `structure.ts`：局部颜色边缘权重；按阶段、64px 分析网格区域、实际笔宽及色盘颜色组织连续任务。任务在落笔前安排，每笔仍用真实 Painting 状态复查颜色误差。随后沿用不相交笔触的有限排序。不跨阶段全局集中颜色。
- `materials.ts`：新模式的 24 色上限内加入边缘权重，原版权重保持原计算。最多 8 种实际宽度；笔具和色盘仍对应执行参数。
- 新模式末两阶段使用未额外模糊的分析色，固定上色量 0.9，细化厚度 0.065，保留扁刷刷毛及真实颜色/高度计算。手动引擎和渲染器没有改动。

这不是人物识别、专业画师步骤、颜料耗尽模拟或照片级精修。任务区域是几何网格。新增模式改变计划、色盘和实际作品，不保证与原版状态相同；原版计划和历史执行结果必须保持一致。没有引入模型、接口、依赖、第三方代码或新素材；原三张图许可仍见 SOURCES.md。私人截图、私人图片及其结果均未纳入公开证据。

## 验证记录

| 项目 | 状态 | 依据 |
| --- | --- | --- |
| 类型检查、构建 | 通过 | 最终应用实际执行；`c-regression/typecheck.log`、`final-build.log`（构建含类型检查） |
| 局部任务不丢候选、边缘权重和透明区域 | 通过 | `a-checks/npx-results.json` |
| 新模式确定性、上限、测试时钟三速度和暂停 | 通过 | 同上，合成双区域只用于功能验证，不作画质样本 |
| 原版完整风景计划与历史逐字段相同 | 通过 | `a-checks/switching/results.json` 及对应测试 |
| 模式切换/取消保留、切回继续、新模式实际落笔导出、M2 草稿隔离 | 通过 | `a-checks/switching/process.webm` 与状态记录 |
| 原三张完整实际绘制及独立颜色/高度重放 | 通过 | `b-fixed/npx-results.json`，三张各五阶段 PNG、4× 实际录像、导出无参考层与独立执行状态一致 |
| 完整新计划全部三速度 | 通过 | 三图 × 0.5/1/4×，9 个完整计划时钟测试；`c-regression/full-speeds`，颜色/高度与实际录像结果相同 |
| M1/M2 与既有 E1 引擎回归 | 通过 | `c-regression/npx-results.json` 共 53 项：M1 引擎 7、原 E1 引擎 23、新模式 11、M1 浏览器 4、M2 浏览器 8 |
| 图片实验浏览器异常与控制回归 | 通过 | `c-browser/npx-results.json` 共 6 项：Worker/超时、重复规划、迟到解码、无效/过大文件、取消/更换/退出、原版备料/重播/签名保留、新模式切换及降级续画 |
| 五次加载/退出页面堆与上下文释放 | 通过 | `c-browser/lifecycle/results.json`：6,155,368 → 8,889,700 bytes，约 +2.61 MiB，低于原 24 MiB 门槛；不含 Worker/GPU/原生总内存 |
| 新模式上下文丢失、降级导出与继续绘画 | 通过 | `c-browser/switching` 的状态、实际 PNG 和录像；原版/M1 降级也在本轮回归通过 |
| 独立连续性能 | 通过 | `d-performance/continuous.json`：完整复杂场景 4×、无录屏/截图/导出，批次 p95 2.6ms，最大 10.1ms，无连续三批 >100ms |
| 交付入口、媒体与窄视口 | 通过 | `review-check.json`：42 个链接、全部图片，4 段录像首/中/末帧实际解码；1440/700/360 模拟视口无横向溢出，文件提示可读 |
| 人物原图精度、真机触控、真实用户体验 | 未测试 | 未获得对应私人原图/计划，不新增人物样本冒充原问题已解决 |
| M1 V4、M2 人工验收、新模式画作与过程 | 待用户确认 | 自动测试不替代人工验收 |

证据独立存于 `artifacts/e1/structure-mode`，不覆盖任何旧图片、固定参数、视频或报告。

## 同图结果与过程变化

下表为完整计划的实际次数，不是预计节省。原版取自此前固定记录，新模式输入 SHA-256、构图及 seed 均相同。规划参数和实际颜色有变化，因此不要求新旧画作相同；各自的确定性另外验证。

| 样本 | 原版笔触 → 新模式 | 原版取色 → 新模式 | 每次取色平均笔数 | 只画一笔的批次占比 | 换笔次数 |
| --- | --- | --- | --- | --- | --- |
| 风景 | 7046 → 7333 | 3832 → 2103 | 1.84 → 3.49 | 64.4% → 37.2% | 691 → 249 |
| 静物 | 7305 → 7440 | 5193 → 2485 | 1.41 → 2.99 | 78.3% → 39.5% | 799 → 218 |
| 复杂场景 | 7240 → 7315 | 4485 → 2316 | 1.61 → 3.16 | 71.5% → 40.4% | 765 → 261 |

取色次数分别减少约 45.1%、52.1%、48.4%，但笔触数略增，且仍有约 37%–40% 的批次只画一笔。不能把次数变化直接等同于同幅画的墙钟加速比例或质量提升。

三图完整录像为 4× 实际页面播放，包含五次阶段截取与导出停顿；不是合成时钟录像。录屏期间规划分别约 5.69/7.42/9.52 秒，绘画批次 p95 为 6.1/6.3/6.5ms，均无连续三批 >100ms。以上带录屏开销，不代替独立性能测量。

实际检查结果：风景的雪地、树群和前景布局仍可辨；静物杯口、杯柄及杯碟关系保留；复杂场景仍能辨认街道、建筑和灯光布局。局部色调、覆盖和轮廓与原版不同，并非每个局部都更好。风景细枝仍碎、咖啡豆仍块状，复杂场景招牌文字和车辆细节还原**未通过**精细还原要求。没有证据宣称该模式已经解决用户人物截图的问题。

完整对照见本地 `http://127.0.0.1:5174/artifacts/e1/structure-mode/index.html`，同时提供原图、原版、新版、五阶段、实际 PNG、计划、输入和完整录像。开发页内也有“查看原版与新模式同图对照”链接。

三图完整录像来自第一份可运行模式，之后只收紧模式栏、补对照链接及修正窄视口文件提示，没有再改规划、画笔或渲染。最终切换操作录像与准备截图见 `c-browser/switching`。窄视口初次自动检查虽无溢出，但查看截图发现文件提示竖排，已修正并增加可读宽高检查；前次截图和检查记录保留在 `review-r1`。所有视口检查均为浏览器模拟，不是真机体验。

实际环境为 Windows、Chrome 152（UA 报告 Windows NT 10.0），WebGL2 / ANGLE Intel UHD Graphics。自动作品颜色/高度 6 MiB、复用笔前颜色缓冲 4 MiB、历史 0；另有渲染 CPU 缓冲 4 MiB、GPU 纹理约 6 MiB。这些显式缓冲统计不包含浏览器全部内存。

独立连续测量运行时没有并行本轮其他测试：复杂场景 7315 笔、4×，规划约 4.95 秒，含规划的完整测量时长 229.34 秒。全部 13,303 个绘画批次纳入统计，p95 2.6ms、最大 10.1ms、>100ms 为 0；规划期帧间隔 p95 4.3ms、最大 33.3ms。播放期帧间隔 p95 4.4ms，但有一次 **579.1ms** 间隔，PerformanceObserver 未记录对应长任务，原因未确认。通过的是既有绘画批次门槛，不能声称零卡顿，也不能把帧间隔当成输入延迟。三图录像时的较慢耗时含录屏、阶段暂停与导出，不与此数据混算。

本轮应用实现与最终验证均完成。`056a723` 为可选模式，`b8e5f0c` 为三图证据，`7c59dc7` 为回归及紧凑入口；各步均已推送 E1 分支。最终性能资料与本文一并另行提交推送。不合并 main、不触发部署，交付后停止，等待用户确认画作和过程。

## 已知限制与停止点

结构优先只是可选实验，不是已证明全面优于原版。局部批次有望减少取用，但会改变覆盖顺序和颜色分布，必须展示同图对照；减少动作不代表人物相似度提升。全局有限色盘、512 分析及最小 4px 扁刷仍限制小五官、细字和复杂边界。颜色边缘也可能把背景细碎纹理排在重要语义之前。

本轮不实现模型识别、多风格商店、图片持久化或中途手动接管。保持独立实验退出前导出提示。性能门槛沿用批次 p95 ≤50ms、不得连续三批 >100ms；录屏指标与独立测量分开。当前工作范围内最多两轮聚焦修正，不能换测试图片掩盖问题。
