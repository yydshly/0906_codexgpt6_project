# M0 调查结论与 M1 待确认方案

日期：2026-09-06。状态：M0 完成；M1 尚未批准、未实现。

本轮仅新增本计划。已阅读任务包 README.md、PRODUCT.md、EXPERIENCE.md、ACCEPTANCE.md、AGENTS.template.md，已实际查看 reference/reference.png，也阅读了根目录汇总稿（读取时名为 oil_painting_product_plan.md；结束复查时该路径不再存在，另有 oil_painting_product_plan_forread.md，本轮未操作这些文件）。产品与验收依据采用任务包原文；汇总稿不另立范围。

## 1. 仓库调查：有资料，无应用代码

| 项目 | 本轮实查结果 | 对 M1 的影响 |
| --- | --- | --- |
| 工作目录 | E:\0906_codexgpt6_project | 在此目录建立应用，保留资料 |
| Git | main；尚无任何提交，git ls-files 为空 | 不能把资料当成已提交基线 |
| 远端 | origin = https://github.com/yydshly/0906_codexgpt6_project.git | 本轮未拉取新内容、提交或推送 |
| 当前文件 | 任务包五份 Markdown、一张参考 PNG，以及根目录产品汇总稿；均为既有未跟踪文件 | 全部保留，不覆盖、不删除 |
| 既有规则 | 在仓库内含隐藏文件的扫描中未找到 AGENTS.md；E:\AGENTS.md 也不存在 | 没有可合并的现有项目规则；第 8 节列出拟新增规则，本轮不创建 AGENTS.md |
| 技术栈与启动 | 无 src、package.json、锁文件、应用配置、启动脚本或测试 | 从业务代码角度是空仓库；目前没有可运行地址 |
| 项目依赖 | 从项目目录无法解析 react、vite、typescript、playwright、@playwright/test | 不能声称依赖可直接复用；M1 再做最小安装 |
| 可复用资产 | 已确认的定位、范围、验收标准、参考图 | 可复用规格与视觉方向，没有现成绘画引擎、组件或设计系统 |

环境实查：Windows 11 家庭版中文版 64 位，10.0.26200；Node v22.15.0、npm 10.9.2、Git 2.42.0.windows.2。pnpm 命令可发现，但未验证，计划统一使用 npm。

Chrome 152.0.7977.82、Edge 152.0.4191.62 的安装文件存在。系统列出 i9-13980HX、Intel UHD Graphics 和 RTX 4070 Laptop GPU。这仅证明系统设备与浏览器文件存在，不证明 WebGL 可用、浏览器启用硬件加速或实际选择了哪块 GPU。未运行绘画页面、浏览器自动化、压感测试或性能基准。

已执行的调查包括：Get-ChildItem -Force、rg --files --hidden --no-ignore（排除 .git）、git status --short --branch、git log、git ls-files、git remote -v、工具版本查询、Node 模块解析检查、浏览器文件版本和系统硬件查询。git log 报告 main 尚无提交，符合上述状态。

未发现用户任务与现有产品资料的冲突。任务包明确要求先批准再执行 M1，与本轮指令一致。

## 2. 用一次首次使用说明产品目标

没有绘画基础的小林旅行回来，想亲手留下一幅日落纪念画。她打开网页，看到暖灰桌面上一张空白织纹画布和右侧木质色板，直接选择“画一幅旅行日落”。提示建议先铺天空，她点暖黄色，用宽平头笔横扫；画布上立即出现跟手、有刷毛边缘和细小颜料起伏的真实笔触。

她换蓝色画水面，在交界处轻轻混合，又用较高上色量盖住不满意的位置。远山画得太高，她撤销这一笔后重新画。随后她关闭参考提示，自己加一条反光；如果中途离开，回到已经成功保存的作品继续画。最后签名、预览，把实际画出的作品导出为 PNG。她愿意保存它，因为配色、位置和最后那条反光来自自己的选择。

这描述的是产品完整首轮体验。其中主题分步引导、参考层、保存恢复、签名和完成预览属于 M2；M1 只验证同一画室页面里的真实创作闭环与最低油画观感。首次首笔 60 秒内、完成 10—15 分钟是后续实测目标，本轮没有用户行为证据。不加入账号、支付、社区、照片自动转换或专业图层系统。

## 3. 参考图的设计转译与 M1 页面

实际图像中，浅暖色桌面承托左侧偏下的方形画布，右侧竖向圆角木板布置颜料，顶部是狭长暖金色工具条。画布织纹、边缘厚度与朝同一方向的接触阴影建立材料感；色板上的颜料有不规则轮廓与高光。底部可见视频进度条和播放控件，它们不属于拟实现产品。

M1 采用相同的视觉主次与材料语言，自行设计桌面布局：1440×900 基准下，居中的创作工作区以左侧约 700 CSS 像素方形画布为主体，右侧约 200–240 像素宽木质色板，中间留出操作空间。顶部紧凑工具条承载撤销、大小、上色量、覆盖/混色选择和“导出 PNG”；清空放在次要位置并有确认。最终尺寸随视口调整，不改变 1024×1024 逻辑画布。

色板提供六个命名色：暖白、日落黄、赭黄、朱红、群青、深褐；当前选色有清楚的选中标识与文字反馈。这里的“取色”指点击色板选择颜料，不扩展屏幕吸管或专业色彩面板。一支平头笔，大小初拟 8–96 逻辑像素，上色量 15%–100%，默认中等大小、中等上色量。两个绘画模式使用同一支笔与同一作品状态。

画布附近只保留一句起步提示，例如“选一种颜色，在画布上拖动开始”。M1 不出现未实现的主题、签名、保存、分享或课程入口；不会伪装已自动保存，离开可能丢失当前内存作品的说明放在适当位置。窄桌面布局将色板收紧或移至画布下方，工具仍可使用；不据此宣称手机或平板真机适配。

桌面、木纹和颜料按钮用原创 CSS/SVG 或小型程序纹理实现；画布织纹进入实际材质渲染。参考 PNG 仅用于审阅，不进入应用背景、画作或导出。页面材料阴影与画布内部笔触光照各司其职。每个可用按钮都有真实行为、键盘焦点和明确状态。

## 4. 单一最小主技术路线

选择 React + TypeScript + Vite 的本地前端应用，不需要后端或运行时模型接口。以下是待验证的工程方案，不是对参考产品实现方式的判断。

| 职责 | M1 方案 | 边界 |
| --- | --- | --- |
| 页面视觉 | React 管理工具与状态；CSS/SVG 实现布局、桌面、木板和控件 | 不让 React 每个指针采样都触发组件更新 |
| 绘画引擎 | 独立 TypeScript 模块，Pointer Events → 逻辑坐标 → 距离重采样 → 定向刷毛印章 → 局部像素更新 | 所有笔触本地计算，不调用大模型 |
| 材质渲染 | CPU 维护颜色/覆盖与高度；原生 WebGL2 上传变化区域，由高度梯度生成法线，统一柔和光照显示笔触和织纹 | 2D 状态加高度观感；不引入 Three.js、完整 3D 房间或流体求解器 |
| 作品数据 | 1024×1024 RGBA8 颜色/覆盖数组 + Uint16 高度数组；有限笔前快照用于撤销 | M1 仅会话内编辑和 PNG；可继续编辑的本地持久化留在 M2 |

运行依赖限 react、react-dom；开发依赖限 Vite、TypeScript、React 插件/类型和必要的 Playwright 测试工具。具体版本在 M1 建立时核对兼容性并锁定，保留 package-lock.json；本轮没有安装。Node 22.15.0 符合目前 Vite 文档列出的 Node 22.12+ 基线，但仍需核对所选模板依赖。[Vite 官方启动文档](https://vite.dev/guide/)

### 4.1 输入与笔触

以画布内容区域的 bounding rect 转换到 1024×1024 坐标；边框、CSS 缩放与设备像素比不进入作品坐标。按路径距离补点，间距初拟不超过笔宽的 1/8，小笔另设下限；轻度平滑方向，避免快速转弯处方向突然翻转。

平头印章以切线方向定向，横截面用固定种子的刷毛束、间隙和轻微长短差形成变化；上色量影响覆盖与高度增量，尺寸只改变宽度。高度有上限，重复涂抹不能无限抬升或越来越亮。刷毛噪声沿一笔保持连贯，不在每帧重新抽样，避免闪烁和随机圆点。

按 pointerId 捕获并处理主指针；只接受明确的绘画按键，忽略额外指针。支持时读取合并采样，否则使用普通 pointermove 加重采样。该 API 并非所有浏览器均支持，必须特性检测。[MDN 合并采样](https://developer.mozilla.org/en-US/docs/Web/API/PointerEvent/getCoalescedEvents)

正常抬笔提交一笔。移出画布时裁剪路径，外部抬笔仍正确结束；pointercancel、失焦、捕获意外丢失或绘制中布局突变时，结束并提交已经画出的部分，不补画远处连接线、不持续误绘，随后可整笔撤销。事件结束处理保持幂等。鼠标采用固定压力；触控笔只有收到有效硬件压力时才使用，未真机测试就标未测试。鼠标事件中的默认压力值不等于真实压感。[MDN pressure](https://developer.mozilla.org/en-US/docs/Web/API/PointerEvent/pressure)

### 4.2 覆盖与基础混色

给用户明确的“覆盖 / 混色”二选一，默认覆盖。覆盖按局部刷毛遮罩和上色量叠加新颜料；高上色量更接近新颜色，低量露出底色。混色在已有颜料的接触区域，使用小型、自实现的 RYB（红黄蓝艺术家色轮）近似映射，按新颜料量和底层覆盖求混合色，再按遮罩写回；空白处仍铺选中颜色。同一混色模式下不额外模拟跨区域携带颜料、干燥时间或液体扩散。

RYB 近似是本方案的实验假设，具体映射须随固定样本记录，不是物理色彩标准。不得只为黄蓝样本硬编码一个绿色；白色提亮、同色重叠、黄红与红蓝也需验证。固定黄蓝中量交叉期望得到可辨识的偏绿接触带，高量覆盖期望更接近后落颜色，交叠外颜色不变。颜色变化只能来自实际接触计算，不能靠整画布模糊。若该近似难以同时满足这几组关系，应按第 5 节停止评估，不无声接入新引擎。

### 4.3 材质与性能约束

颜色和高度是唯一绘画状态；法线从高度和固定织纹推导，不额外保存一套不可撤销的阴影。厚涂稍遮盖织纹，薄涂透出底材。高光克制且与局部方向一致，避免塑料浮雕。用关闭局部高度光照的诊断对照证明效果来自笔触；此开关仅用于开发测试。

CPU 只处理笔刷接触区域；requestAnimationFrame 合并更新与纹理上传，预分配刷毛遮罩和缓冲区。选择 CPU 作为状态来源，是为了先降低混色、撤销和上下文恢复的复杂度；大笔与混色可能成为性能瓶颈，必须实测。绘画期间不逐采样读取 GPU，也不逐采样复制全画布。限制纹理数量、背缓冲尺寸和 DPR，避免无界资源开销，这与 WebGL 的资源预算和避免阻塞调用建议一致。[MDN WebGL 实践](https://developer.mozilla.org/en-US/docs/Web/API/WebGL_API/WebGL_best_practices)

基础颜色和高度均保持原生 1024×1024，显示背缓冲每边最多 1024，显示 DPR 上限 1.5。可降低显示分辨率、光照细节和装饰性效果，不能降低工作色层后再放大导出、不能删掉所有刷毛特征来报通过。输入队列要有界；固定距离插值保持笔迹连续，若长笔队列持续积压须计入性能失败，不无限缓存等待。

### 4.4 撤销、数据与 PNG

M1 选简单、有界的笔前完整状态快照，不先实现通用事件回放系统。每份快照包含 RGBA8 颜色/覆盖和 Uint16 高度，约 6 MiB；保留最近 20 笔共 120 MiB，进行中另有一份约 6 MiB 的待提交快照，历史池不超过 126 MiB。当前状态、渲染上传暂存、导出缓冲另行核算，绘画模块 CPU 显式缓冲总预算初定 160 MiB，GPU 纹理另报；这些是预算计算，不是实测进程内存。

每笔开始最多拍一次快照，空笔不入历史，结束后进入有界队列并释放最旧状态。撤销直接恢复两个数组并更新全部必要材质区域；不依赖 GPU 重新混色或跨浏览器重放。撤销后新画从当前状态分叉；M1 不显示重做按钮。清空需要确认，将其作为一个可撤销操作；取消清空不改变作品与历史。满历史时也必须保留最近 20 个操作。分配失败不得默默清空作品或缩减至不足 20 笔后宣称合格。

作品头保留 formatVersion、brushVersion、逻辑尺寸、画布纹理种子与固定光照配置；每笔固定种子用于测试复现。M2 再定义持久化的版本格式、事务和保存状态，不提前建数据库或宣称刷新可恢复。

导出等待当前绘画事务结束并固定状态，在独立 1024×1024 渲染目标上使用同一材质参数生成图像，再编码 PNG。包括实际笔触、画布底色/织纹及局部材质；不包括桌面、工具栏、鼠标光标或参考图。导出期间显示真实处理中/失败状态，避免生成空白或半笔文件。恢复撤销后的作品导出也须一致，不通过截整个网页裁图代替。

WebGL 不可用或上下文丢失时，保留 CPU 作品和历史，用 Canvas 2D 展示颜色并继续基础绘制、混色、撤销与导出；明确提示当前简化显示。恢复 WebGL 时重新上传颜色与高度。降级 PNG 与降级预览一致，并明确缺少正常档局部光照；降级通过只证明容错，不代表油画视觉验收通过。M1 的内存恢复不等于页面刷新恢复。

## 5. 最大风险、技术验证范围和停止条件

以下验证全部属于批准后的 M1，本轮不写原型。第一份可运行页面就同时具有上述视觉设计与全部基本操作；技术诊断在同一引擎上做，不将独立普通画板当作交付。

| 风险 | 有限验证范围 | 通过依据 | 停止条件与处理 |
| --- | --- | --- | --- |
| 笔触像圆线、橡皮条或塑料 | 固定单笔、交叉、叠涂三套样本；8/32/96 像素笔宽；低/中/高上色量；相同视角与光照 | 刷毛方向沿路径，边缘有连贯变化，局部厚度由实际笔触产生；产品负责人认可最低观感 | 第一可运行版后最多两轮聚焦视觉修正；仍不能辨认核心特征或需改主路线，提交相同样本的差异和替代方案，停止等待决定 |
| 基础混色发灰、发黑或不可预测 | 黄蓝交叉，交换先后顺序；同色、白加蓝、黄加红、红加蓝；覆盖/混色两个模式；重复 1/5/10 次 | 接触外颜色不变；模式有可解释区别；中量黄蓝呈偏绿，白提亮，同色不无故变脏；重复叠涂有界 | 两轮参数/映射修正仍不能同时满足样本，不通过改色卡或更换样本掩盖问题；不扩成光谱/流体研究，报告后停止 |
| CPU 像素混合或上传卡顿 | 1440×900、1024 工作画布；32 和 96 像素混色分别连续 60 秒；首次/满 20 笔历史均测 | 报实际平均 FPS、p95 帧间隔与掉帧/积压；理想接近 60 FPS，最低评估档平均 ≥30 FPS 且 p95 ≤50ms，不能明显持续追赶指针 | p95 门槛为本方案拟定的试验口径，待用户确认；同一路线最多两轮针对热点修正，仍不达最低档或需迁移 GPU 绘画/Worker 核心处理时，先展示证据，停止等待路线确认 |
| 撤销残留高度、满历史失效 | 20 笔逐一撤销；第 21 笔后撤 20 次；撤销再画；清空取消/确认/撤销；含最大笔宽重叠 | 颜色与高度原始数组对笔前状态逐字节相同；材质截图无幽灵高光，导出对应当前状态 | 任一核心状态不一致直接判未通过，阻止 M1 交付通过；先修状态事务，不继续 M2；若需替换历史模型，报告预算和原因 |
| 内存或长笔队列无界 | 固定 20 笔→撤销→清空→重画循环 5 次，另测 60 秒不抬笔；记录显式分配与可得的进程/堆趋势 | 历史池和显式缓冲不超预算；临时资源释放后不随轮次持续增长，队列不持续积压 | 超预算或增长无法解释时判失败；不得通过把 20 笔改成更少解决验收 |
| 导出和异常输入丢作品 | 等待笔结束导出；缩放/布局变化；移出抬笔；失焦/cancel；GL 不可用、丢失及恢复 | 正常 PNG 1024×1024 可打开，与当前正常渲染一致；异常后不误绘，恢复不丢颜色/高度 | 白屏、空导出、坐标漂移或丢已完成笔触均阻止通过，不能只做截图交付 |

性能记录要写 CPU/GPU、浏览器、系统、视口、逻辑尺寸、DPR、背缓冲尺寸和是否软件渲染。当前机器配置较高，在此通过不外推低端设备。计时采用预热后无录像的 rAF 采样，报告平均 FPS 的计算方式与 p95；自动化输入、后台标签页等干扰需记录。rAF 帧间隔不代表端到端输入延迟，手感另由实际鼠标操作评价。

试验预算按“初版 + 最多两轮聚焦修正”控制，而非工期承诺；同一轮可同时修复笔触和混色，不为每个风险重复追加两轮预算。功能正确性有缺陷必须据实标失败；不能因试验预算到期就宣称通过。

## 6. 批准后的执行顺序与最小交付

1. 重新核对仓库变化，按确认差异建立或合并项目 AGENTS.md，保留全部既有资料。锁定 npm 依赖，建立最小 Vite/React/TS 项目与本地启动说明。
2. 冻结固定测试轨迹、颜色、种子和参数；在设计过的画室页面内串起取色、真实平头笔、两种叠色规则、高度显示、撤销、PNG 导出。共享一个绘画状态，不使用预制成品。先让真实落笔成为迭代依据。
3. 执行有限技术验证：查看三类样本及实际绘制手感，测大笔混色性能和历史预算。优先修最影响笔触和混色的可观察差异；若触发停止条件，提交证据停下。
4. 完成输入异常、尺寸变化、清空确认、降级/恢复和导出错误处理。跑全套 M1 功能与视觉证据测试，并单独测性能。
5. 汇总 F1–F8、V1–V4、性能与降级报告；交付页面截图、固定样本、操作录像、实际 PNG 和原始测试结果。人工视觉项交由用户确认；M1 交付后停止，不进入 M2。

拟建立的源码职责为 ui、painting/input、painting/brush、painting/mix、painting/history、rendering、export；只在复杂度确实需要时拆文件，不建设插件化引擎。测试轨迹放 tests/fixtures，验收证据放 artifacts/m1，来源与使用条件记入 SOURCES.md。这些路径目前都只是计划，尚未创建。

## 7. 验收命令、操作与证据

当前没有 package.json，以下命令是 M1 必须提供的命令约定，本轮未运行，不能现在视为可用启动方式。M1 首次初始化时安装锁定的最小依赖；有锁文件后的复现流程如下：

```powershell
Set-Location E:\0906_codexgpt6_project
npm ci
npm run typecheck
npm run build
npm run test:engine
npm run test:e2e
npm run test:perf
npm run dev -- --host 127.0.0.1 --port 5173 --strictPort
```

拟定 dev 地址为 http://127.0.0.1:5173；必须以实际启动日志核验，端口被占用时明确报告。typecheck 执行 TypeScript 检查；test:engine 验证真实状态/混色/坐标不变量；test:e2e 用 Playwright 驱动 UI 并保留成功与失败证据；test:perf 单独运行无录像的基准。测试脚本需自动管理本地服务器，不要求用户先手动保持另一个 dev 窗口。

优先复用已安装 Chrome/Edge，批准后检查 Playwright 驱动与录像编码能力；必要时只准备一个 Chromium 目标及其必要组件，不默认下载全浏览器矩阵。本轮仅查到浏览器文件，自动化启动、录像与 GPU 能力均未测。配置录像为 on，显式设 1440×900，关闭 browser context 后确认录像落盘；成功录像也保留。[Playwright 官方录像文档](https://playwright.dev/docs/videos)

| 验收项 | 可复现操作 | 要保留的证据与判定 |
| --- | --- | --- |
| F1 / V1 | 本地启动，1440×900 打开；检查首屏和控制台 | page.png、启动/构建日志；设计主次、木板/织纹/光照对应合理，无阻止创作的错误 |
| F2 / F4 | 点黄色拖出一笔，换蓝再画；分别改变大小和上色量 | 鼠标真实轨迹与结果；参数各有独立可见影响，抬起即停 |
| F3 | 慢直线、快弧线、急转弯、连续画；移出画布外抬笔、触发 cancel/失焦 | 输入测试记录及录像；无断笔、明显偏移和持续误绘；合成 cancel 与人工失焦分别标注 |
| F5 / V2 | 运行下述固定样本，比较覆盖/混色、接触内外像素 | single-stroke.png、two-color-overlap.png、repeated-layering.png，加参数文件与原始颜色/高度校验；不能只交空画布 |
| F6 | 画 20 笔，每笔记录前后状态，逐步撤 20 次；再测 21 笔只撤最近 20 笔 | undo-results.json、关键截图；颜色/高度完整恢复，第 21 笔场景应留下第一笔 |
| F7 | 正常绘制并撤销一次后点导出，再重新打开下载文件 | actual-painting.png；解码后 1024×1024，验证非空、方向正确、无 UI/参考图，与导出时作品一致 |
| F8 | 1440×900 改至 1100×760，再改回；测试浏览器缩放与绘制中 resize | resize 截图与坐标检查；既有数组不变、新笔位置正确，无拉长连接线 |
| V3 | 一段连续真实 UI 操作完成取色→绘制→覆盖/混色→撤销→导出 | workflow.webm 和对应下载 PNG；浏览器下载事件/文件检查补足视频无法证明的文件内容 |
| V4 | 产品负责人查看真实页面、固定样本和录像，并亲自试画 | 在 report.md 记录用户判断；未确认则标未测试/待人工验收，不自评拟真百分比 |
| 性能/内存 | 大小笔 60 秒基准、满历史基准、5 轮历史循环与长笔 | perf.json、memory.json、environment.json；实测均值/p95、显式分配及测量限制 |
| 降级 | 测试模式拒绝 GL；扩展可用时模拟 context loss/restore；期间画、撤、导出 | fallback 截图、恢复前后状态校验；不白屏；简化视觉与正常视觉分别报告 |

固定样本初始定义（1024 逻辑坐标；seed=906；后续不得换样本掩盖失败）：

- 单笔：群青 #3155A6，宽 32，上色量 60%，从 (192,320) 慢画到 (832,320)，独立补测同端点快速路径与弧线；干净画布开始。
- 两色交叉：先以日落黄 #EBC43C、宽 64、上色量 75% 从 (192,512) 到 (832,512)，再用群青、宽 64、上色量 50% 从 (512,192) 到 (512,832)；两种模式分别从同一底图执行，并交换颜色顺序。
- 重复叠涂：在同一片区域以宽 48、上色量 50% 按固定短弧轨迹交替黄蓝，保存第 1、5、10 次结果；检查高度上限、刷毛边缘与接触范围外的颜色不变。轨迹点列在 M1 写入 fixture 并锁定。

预定证据目录为 artifacts/m1/，报告逐条写“通过 / 未通过 / 未测试”，附环境、复现命令、证据相对路径与已知限制。文件名以上述约定为准，任何未生成的证据不得占位冒充。自动化图片差异用于回归，同一 GPU/浏览器以外不承诺像素级渲染一致；CPU 撤销状态仍应精确一致。

本轮验收状态：M0 阅读与调查已完成；M1 的 F1–F8、V1–V4、性能、降级、鼠标/触摸/触控笔全部未测试。没有应用可运行，因此不执行构建、不生成虚假画作或录像。

## 8. 拟写入 AGENTS.md 的规则差异

本次扫描没有既有 AGENTS.md，因此差异为“新增项目规则”，没有要删除或替换的旧条款。本轮只列差异，不创建规则文件。M1 批准后先重新检查是否已有其他人新增规则；若有则逐条保留不冲突内容，仅合并确认项。

| 规则组 | 拟新增的可执行规则 | 来源/适用阶段 |
| --- | --- | --- |
| 产品上下文 | 开始相关工作先读 PRODUCT、EXPERIENCE、ACCEPTANCE；已确认定位和边界优先，不扩成专业平台或静态页 | 模板原意；全部阶段 |
| 里程碑 | 只执行明确批准的 M0/M1/M2；M1 之后停等验收；关键假设失败、付费能力或更换主路线时先说明并等待决定 | 模板 + 本轮用户指令 |
| 真实交互 | 第一可运行交付同时具备设计页面和真实绘画闭环；禁止截图背景、成品揭幕、视频或假按钮冒充功能 | 模板 + EXPERIENCE |
| 职责和范围 | 输入/绘画/UI/材质/作品数据分离；落笔本地计算；不加账号、支付、社区、任意照片转换、3D 房间或无关重构 | 模板 + 已定产品范围 |
| 笔触与数据 | 固定 1024 工作画布；最近至少 20 笔撤销同时恢复颜色和高度；逻辑坐标独立；固定随机种子与版本；导出只含实际作品 | EXPERIENCE + 本方案技术补充，待确认 |
| 试验预算 | 固定样本与参数；初版后最多两轮聚焦视觉修正；达不到核心门槛或需要改路线则报告差距后停止，不修改基准消除失败 | README + 本方案第 5 节 |
| 测试证据 | 真正运行构建、测试和页面；逐项报告通过/未通过/未测；提交固定样本、真实操作录像和 PNG；视觉与手感由用户确认 | 模板 + ACCEPTANCE |
| 环境诚实 | 缺浏览器/GPU/真机时明确未测并给步骤；记录实际环境与性能；不把模拟视口当真机、不把帧率当输入延迟 | 模板 + ACCEPTANCE |
| 保护资料与作品 | 保留未提交文件和用户作品；不擅自清仓库历史；清空需确认；不把密钥放前端、不自动上传图片、不自动开通收费服务 | 模板 + EXPERIENCE |
| 来源 | 新依赖、纹理、代码记录来源和使用条件；参考图仅作参考；第三方内容不覆盖项目执行边界 | 模板 + EXPERIENCE |
| 保存承诺 | M1 不声称刷新恢复；M2 区分保存中/成功/失败，保存可继续编辑的数据，本地存储不等于永久备份 | 模板 + M1/M2 分界 |
| 交付 | 说明目标、文件与原因、运行步骤、测试结果、证据、限制和停止点；不把能运行写成油画效果已通过 | 模板 |

等待确认的具体方案就是：上述同页 M1 范围、React/TypeScript/Vite + CPU 绘画状态 + WebGL2 材质主路线、基础 RYB 混色验证、20 笔有界快照、性能试验门槛与两轮修正预算，以及本节规则差异。确认后才开始 M1。
