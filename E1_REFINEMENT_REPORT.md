# E1 细节与可见单笔过程补正

基线为 codex/e1-image-painting / 7de403e，main 与线上 M2 保持不变。用户确认先做成品细节和可见笔尖过程；多风格、实验恢复、语义画师步骤留作后续。

## A：第一轮细节修正

分析尺寸由 256 提高到 512，原生画布仍为 1024。五档笔宽 72/36/16/8/4，笔触上限 8020，seed 1906 不变。局部梯度保护轮廓，空间配额避免细节预算只集中于少数区域，边缘附近缩小笔宽并截短路径。实验显式采用薄颜料高度；手动默认参数、混色、20 笔撤销与旧实验计划保持原结果。

透明留白不再铺满浅色笔触；画布材质来自原渲染器。照片不写入作品，规划和播放仍调用同一个 Painting，规划器版本 e1-refined-2 / 画笔参数版本 2。固定路径采样为后续笔尖逐点播放准备，不随速度变化。

通过：typecheck、build、原引擎 7 项、新实验引擎 4 项、三张原照片的五阶段真实输出/PNG/独立 CPU 重放/原画室隔离与批次性能门槛。三张旧 E1 计划在新引擎上颜色和高度 SHA-256 全部保持历史值。构建只产生本地 dist，没有发布。

未通过（已定位并复验）：首次浏览器执行三张照片均规划失败。开发服务缓存旧分析代码（256），Worker 已读取新规划器（512），导致尺寸不一致；重启服务后相同源代码和样本三项通过。失败原始测试记录在 refinement/r1，成功证据在 refinement/r1-verified。此为接入错误，没有更换样本或降低门槛。

视觉观察：留白的重划痕消除，局部边缘和色彩细节增加；仍是概括式油画。风景树枝、静物豆粒内部结构及复杂场景车体/灯牌仍有明显失真。数值通过不代表质量通过。保留第一轮全部输出，本次暂不消耗第二轮效果优化预算。

## B：笔尖过程

开发中，尚未交付或验收。计划让笔尖位置对应真实路径采样，支持笔内暂停并确保导出不会结束当前笔；完成后记录实际测试与过程录像。

M1 V4、M2 完整体验、E1 画作与过程人工验收均为待用户确认。
