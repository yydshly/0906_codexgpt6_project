# E1 细节与可见单笔过程补正

基线为 codex/e1-image-painting / 7de403e，main 与线上 M2 保持不变。用户确认先做成品细节和可见笔尖过程；多风格、实验恢复、语义画师步骤留作后续。

## A：第一轮细节修正

分析尺寸由 256 提高到 512，原生画布仍为 1024。五档笔宽 72/36/16/8/4，笔触上限 8020，seed 1906 不变。局部梯度保护轮廓，空间配额避免细节预算只集中于少数区域，边缘附近缩小笔宽并截短路径。实验显式采用薄颜料高度；手动默认参数、混色、20 笔撤销与旧实验计划保持原结果。

透明留白不再铺满浅色笔触；画布材质来自原渲染器。照片不写入作品，规划和播放仍调用同一个 Painting，规划器版本 e1-refined-2 / 画笔参数版本 2。固定路径采样为后续笔尖逐点播放准备，不随速度变化。

通过：typecheck、build、原引擎 7 项、新实验引擎 4 项、三张原照片的五阶段真实输出/PNG/独立 CPU 重放/原画室隔离与批次性能门槛。三张旧 E1 计划在新引擎上颜色和高度 SHA-256 全部保持历史值。构建只产生本地 dist，没有发布。

未通过（已定位并复验）：首次浏览器执行三张照片均规划失败。开发服务缓存旧分析代码（256），Worker 已读取新规划器（512），导致尺寸不一致；重启服务后相同源代码和样本三项通过。失败原始测试记录在 refinement/r1，成功证据在 refinement/r1-verified。此为接入错误，没有更换样本或降低门槛。

视觉观察：留白的重划痕消除，局部边缘和色彩细节增加；仍是概括式油画。风景树枝、静物豆粒内部结构及复杂场景车体/灯牌仍有明显失真。数值通过不代表质量通过。保留第一轮全部输出，后续第二轮仅针对笔尖显示与性能，不再次修改画作参数。

画面边界处实际刷毛会少量伸入留白，未把作品裁成一张硬边照片。当前只有这一种表现；细腻、松弛、厚涂等可选择风格尚未实现。

## B：笔尖过程

已接入可见笔尖、落笔/抬笔、路径内暂停/继续。每个计划的路径展开为固定采样点，同一笔保留其内部混色状态；帧时间和速度仅决定本次执行多少采样点。抬笔的显示移动不写作品，完成后笔尖消失。播放每帧最多约 6 ms 工作预算，不为每笔增加全图撤销。

通过：实验引擎 5 项（含混色路径逐点执行、0.5/1/4 速度与暂停一致性），浏览器笔内暂停/导出/继续/替换取消/材质降级/原草稿隔离测试。原采样计划在可见笔尖完成后的最终数组仍与 A 的独立结果完全相同。PNG 不包含笔尖，导出和上下文中断不结束实验活动笔；手动渲染器保留原默认行为。

首次浏览器笔内导出未通过：开发服务再次未收到共享渲染文件的更新，仍执行旧的结束当前笔行为。加入开发源文件轮询并排除证据目录，服务重启且确认返回新模块后复验通过。失败记录 b-checks 与通过记录 b-verified 都保留。这不是把失败状态改成通过，而是分别保存两次实际运行。

证据：b-verified/pen 的暂停截图、笔尖移动截图、实际操作录像、部分 PNG、隐藏笔尖 PNG 与降级 PNG。完整三图可见过程和 M1/M2 回归已在 C 补齐。

M1 V4、M2 完整体验、E1 画作与过程人工验收均为待用户确认。

## C：回归与交付记录

类型检查、生产构建通过；构建是本地验证，不是部署。M1 引擎 7 项、浏览器 4 项和 M2 浏览器 8 项重新执行通过，包含逐笔颜色/高度恢复、错误高度反向验证、UI SHA、草稿恢复与失败、签名真实 PNG、WebGL 不可用/丢失/恢复。E1 的 14 项（5 项引擎、9 项浏览器）全部通过，包含三图五阶段、完整重播、不同速度、笔内暂停/导出、异常与资源释放。独立 E1 性能、M2 绘画/自动保存性能、资料入口检查也已通过。

| 固定样本 | 笔数 | 规划 | 末阶段时间（含规划和取证暂停） | 绘制批次 p95 / 最大 |
| --- | ---: | ---: | ---: | ---: |
| 黄石冬景 | 6735 | 5.39 s | 173.58 s | 6.4 / 13.0 ms |
| 咖啡杯静物 | 7187 | 10.05 s | 169.65 s | 5.6 / 10.6 ms |
| 夜间街景 | 7036 | 7.30 s | 172.06 s | 6.5 / 8.1 ms |

三图均没有连续三个超过 100 ms 的绘制批次；颜色和高度与独立 CPU 重画一致。可见笔尖前后的计划 JSON 与最终 PNG 都完全相同。阶段截图/导出期间仍记录到长任务，不能用批次数据宣称整个页面毫无卡顿。无录像测量另行记录。

三张过程录像分别为 197.60 / 185.08 / 188.44 秒，含浏览器操作与取证暂停；另有 96.20 秒的笔内暂停/导出/降级/继续录像。对照页实际打开了 25 张图片、55 个链接，4 段录像的起点/中段/末段均由 Chrome 解码并读取画面。真实画室入口、实验入口、实验新开资料页后保留原页面、再次选图/绘制/暂停/下载也经过实操检查，见 review-check.json。

五次载图/退出后的页面 usedSize 从 6,048,776 到 8,286,212 字节，增长约 2.13 MiB，backingStorage 变化约 2 KiB；通过原有 <24 MiB 检查。该记录不包含 GPU、Worker 和所有浏览器原生内存，不是整机峰值测试。

### 第二轮局部显示/性能验证与限制

部分 C 长程录像截图中标题区域没有绘出（例如 c/complex/stage-2-page.png），但标题 DOM、可见性与透明度正常。独立短测没有复现，根因未确认。移除笔尖覆盖层的非必要 SVG 全幅投影滤镜，仅保留矢量形状和描边；没有改渲染器材质、画作、规划参数或样本。最终短程页面截图 playback-page.png 标题显示正常，但不能据此宣布此偶发现象已经彻底解决：长期标题显示一致性仍待验证。旧截图全部保留。

显示状态明确区分：C 采集版本的长程标题完整性未通过；最终独立短程显示通过；最终版本的长程标题完整性复验未测试。

同一复杂街景、完整保留、1×、无录像/截图/中途导出：

| 连续测量 | 全程（含规划） | 绘制批次 p95 / 最大 | 播放期 rAF 间隔 p95 / 最大 | 播放期 >100 ms 间隔 |
| --- | ---: | ---: | ---: | ---: |
| 第一轮 performance | 172.48 s | 4.8 / 8.9 ms | 12.4 / 1020.8 ms | 4 |
| 去除滤镜后 performance-verified | 166.17 s | 2.1 / 6.6 ms | 4.3 / 395.8 ms | 1 |

两次批次门槛均通过，无超过 100 ms 的绘制批次。第二次还有规划期 116.7 ms、播放期 395.8 ms 的孤立 rAF 间隔；记录时页面可见且有焦点，未记录到对应主线程长任务，原因尚未定位。不能把单次前后对比当作滤镜是唯一原因，也不能把 rAF 间隔当成端到端输入延迟。未出现连续三个超过 100 ms 的工作批次；未宣称零卡顿或人工手感通过。

第二轮第一次测试因新增资料链接复用样式类而使测试定位器匹配两个段落，未执行完整测量。定位器已精确到状态区域，失败文件 performance-final/test-e1perf-results.json 保留，完整重跑文件为 performance-verified。此为测试定位修正，未调整性能门槛。

M2 满 20 笔历史下的 60 秒真实鼠标绘画与保存间歇复测通过：rAF 平均 238.61 Hz、p95 4.3 ms、最大 75 ms，6 次快照/6 次实际写入，持续落笔期间 0 次草稿全图采集，历史仍为 120 MiB。该刷新频率不代表硬件输入延迟或人工手感。

本次累计一轮细节修正与一轮局部显示/性能验证，预算用完，停止继续调参。M1 完整四组热路径性能基准未重跑，仍引用历史记录；没有以本轮数据覆盖 M1/M2/E1 历史证据。

功能等待上限有明确调整：完整播放等待由 180 秒改为 300 秒，单个包含两次完整播放的用例总时限为 600 秒。原因是此次明确增加抬笔移动与路径内逐点落笔；没有延长 Worker 的 120 秒停止期限，也没有改变批次 p95 ≤50 ms、连续三个 >100 ms 的停止门槛。此处等待上限不是性能通过标准。

### 实现范围与未测试项目

- 照片与 seed 未更换；第一轮画作的计划和 PNG 与可见笔尖版分别做完整指纹对照。三图全部展示，不挑选最好结果。
- 规划仍按像素误差与局部梯度由粗到细，空间邻近区域分组执行。可见笔尖并不提供对象语义、专业起稿/塑形顺序或可靠教学。没有新增模型、依赖、外部服务或上传流程。
- 单个自动 Painting 的当前数组 6 MiB、复用混色缓冲 4 MiB、撤销历史 0；只展开当前一笔的固定采样，统计数组最多 20000 项。手动 20 笔历史仍为 120 MiB 上限。512 分析颜色数组为 1 MiB；规划的模糊中间数组、候选点、Worker 和解码还有额外内存，不把上述缓冲量称为整个浏览器峰值。
- 实验不刷新恢复；退出前仍需导出。没有新增风格、中途接手、任意跳转或视频导出功能。录像只是验收资料。
- 人像、文字、高精度照片还原、整机长期峰值内存、最大尺寸真实照片压力、低配置电脑、真机触屏/触控笔、其他浏览器与真人手感：未测试。复杂场景的细节精确还原：未通过；整体画作质量由用户确认。
- 重放一致性在本机 Chrome 与 Node 的当前引擎版本上实测；没有把本机结果外推为所有浏览器与硬件逐字节一致的承诺。
- M1 完整四组热路径性能基准暂引用上一轮历史记录，没有把它写成本轮重测；本轮重新执行原 M1/M2 功能与降级回归，并针对新的自动绘制单独测量性能。
- 最终仅移除笔尖滤镜并增加本地资料导航后，执行了类型/构建、同一复杂图完整连续性能、M2 连续绘画/保存和实际选图/绘制/暂停/导出烟测；没有为这项显示变化重录三份历史全程视频。三图录像明确对应 C 的采集时版本，实际笔触逻辑和成品未再更改。
- E1 重跑默认结果改到被 Git 忽略的 refinement/local，防止命令覆盖历史 E1/c。默认输出路径已实际执行单项测试验证，default-output-check.json 留证。

### 打开与人工验收

在项目目录执行 `npm run dev`。已运行日志确认本地地址为 http://127.0.0.1:5174/，从右上角「图片自动绘制 · 实验」进入。三图新旧对照入口为 http://127.0.0.1:5174/artifacts/e1/refinement/index.html；该页是资料页，进入实际画室的入口独立且可打开。

1. 先保留或恢复 M2 作品，再进入实验；使用相同三张固定照片，确认完整保留构图。
2. 默认 1× 查看笔尖落笔与抬笔；0.5× 更容易观察单笔，4× 用于较快完成，最终结果应相同。
3. 在一笔中间暂停，导出当前进度，再继续；完成后导出 PNG，确认没有笔尖与参考。
4. 对照原图、旧结果和新结果，评估细节是否够用、笔触是否过粗、过程是否值得观看。检查复杂街景仍未解决的细部，不以通过测试代替判断。
5. 返回画室，原作品和草稿继续保留。本轮完成后等待画作与过程验收。

分步远端记录：2ef5c51（细节与固定样本）、9f9ea6f（真实笔尖与笔内暂停）已推送实验分支；本报告随 C 的最终回归、完整资料和导航提交推送。停止点为等待画作与过程人工验收，不合并 main、不部署、不新增风格或图片扩展。
