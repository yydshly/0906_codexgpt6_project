# E1 成品保真与绘制过程优化分析

日期：2026-09-06。分析基线：`codex/e1-image-painting`，`170bc6e`。本轮只读检查应用与历史计划，新增分析脚本、统计与本文；没有修改应用、引擎、用户作品或部署。

## 结论与保留范围

建议优先解决“关键结构画不准”，同时把规划单位从零散笔触提升为局部绘画任务。过程优化的目标是一次取用笔具和颜色完成一组有用的落笔，而不是让取色动画变快。更少动作与更高保真是两个需要分别验证的目标，不能互相代替。

保留慢光主页面、自由绘画、旅行日落、M2 草稿/签名、手动最近 20 笔撤销、当前图片实验及其已有控制；保留 CPU Painting、WebGL2 StudioRenderer、原生 1024×1024 输出和真实笔触执行。未来试验使用明确的新规划器版本，历史计划仍按原顺序执行。当前能力不撤掉，不把原图贴入画作，不上传私人图片，不接模型或收费服务。

用户人物截图里，脸部五官、头发与脸的交界、衣服领口和手部细节明显缺失，不能仅用“油画风格”解释。这里有保真问题。但目前只有对照截图，没有该私人原图与对应计划，无法据此确认每一处丢失具体发生在哪个处理环节。

## 已核对的代码与统计

代码依据：`src/experiment/plan.ts`、`materials.ts`、`process-plan.ts` 及当前播放流程。统计依据是已有三张固定许可照片的 `prepared-studio/c/*/plan.json`，规划器 `e1-prepared-studio-2`，seed 1906，未重新生成或更换样本。

| 固定样本 | 笔触数 | 取色批次 | 每次取色平均画几笔 | 只画一笔的取色批次占比 | 最后两个细化阶段的笔触占比 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 风景 | 7,046 | 3,832 | 1.84 | 64.4% | 65.6% |
| 简单静物 | 7,305 | 5,193 | 1.41 | 78.3% | 63.4% |
| 复杂场景 | 7,240 | 4,485 | 1.61 | 71.5% | 64.2% |

这些是计划中的参数批次，不是对用户主观流畅度的评分。开始一次取色计入批次；颜色、笔宽或上色量任一变化形成下一批。

- 三图分别有 3,670、5,089、4,349 次批次切换包含颜色变化，颜色交替是主要原因。三图都没有仅由上色量变化造成的切换；上色量变化只在四次阶段交界与颜色、笔宽一起发生。因此应修正“频繁上色量变化导致频繁沾色”的推断。
- 每张准备 24 色、8 种实际宽度。现有代码并非强制每笔沾色，相同颜色/宽度/上色量可以连续画；但生成的序列很少长期保持这一组合。
- 已有局部邻近排序和不相交笔触的有限重排，不是完全随机播放。它发生在笔触生成之后，不能把零碎计划自动变成有明确形体目标的任务。
- 已有粗到细五阶段，且已用真实 Painting 结果反馈颜色误差。问题不是“完全没有反馈”，而是反馈主要看颜色差异，缺少人物关键结构优先级。
- 分析图为 512×512，作品为 1024×1024；全局有限色盘与区域配额没有人物语义。512 分析、有限色盘、局部路径和材质都有可能影响细节，尚不能把某一项判为人物样本的唯一根因。
- 全计划路径长度中位数约 6–7.5 个作品像素。最后两个阶段已占超过六成笔触；不能据此断言所有短笔无用，也不应直接再堆大量小笔。
- 现有上色量是笔触参数，并非模拟笔上剩余颜料。当前流程不能宣称因“颜料实际耗尽”而重新沾色。

原始统计及来源 SHA-256：`artifacts/e1/analysis-task-quality/plan-statistics.json`。其中 128px 网格返回次数只表示几何区域访问，不代表语义对象或重复涂抹；区域/材料组合数忽略遮挡依赖，不能作为可实现的节省次数。

## 成品效果：先定位损失，再分配细节

建议下一次获准试验时，固定输入并对照这条链：原图 → 实际分析图 → 色盘映射结果 → 无材质的作品颜色 → 最终材质结果。中间图仅作为诊断，不进入作品或代替真实落笔。

1. **结构优先。** 先检查脸的朝向、头身比例、发际与脸缘、肩线、领口、手的位置。规划的局部任务应有区域边界与关键边缘，跨越边缘的宽笔缩短或收窄，避免用肤色、发色互相盖掉形体。自动语义识别尚未实现，不能把现有几何网格称为识别了人物。
2. **细节预算按重要性分配。** 人脸、手、衣服轮廓等小区域要能获得额外分析与落笔机会；背景已有足够效果后停止反复细化。先在固定局部验证更高分辨率分析是否真正保住五官，再判断成本，不直接全图扩大计算量。
3. **颜色保留层次。** 检查有限色盘是否合并了重要的肤色明暗、头发边缘和白衣褶皱。考虑按局部任务保留关键色阶，再决定是否需要增加色盘；不是全图每个细微像素差都换一盘颜色。
4. **笔触按区域适配。** 大面用更连续的覆盖，边缘与细节用较短、较窄、较薄的实际笔触。当前扁刷先验证宽度、路径、上色量和厚度的组合，不为解决保真先新增多套风格或笔型。
5. **材质服从结构。** 如果无材质颜色已经失真，优先修规划；如果颜色结果可辨、加材质后才被刷痕掩盖，再测试自动笔触的局部厚度等参数。保留手动画笔行为，不先全局削弱现有油画材质。

拟采用局部误差下降与关键边缘保护共同决定是否继续细化，防止后来的笔触破坏已经画好的区域。是否达到人物相似度要求仍需人看原图与结果，数值只能辅助诊断。

## 绘制过程：阶段 → 区域任务 → 笔具/色盘批次 → 连续落笔

规划时就形成任务，比生成数千笔后仅做排序更值得验证。例如：

> 背景大面：宽笔、背景主色连续覆盖一片区域 → 人物衣服亮面：中笔、暖白连续铺形 → 衣服暗面：中笔、灰色建立褶皱 → 头发形体 → 脸部明暗与关键轮廓 → 少量高光修整。

这只是拟议的组织示例，不是所有人物通用的正确绘画顺序，也不宣称为专业画师教学。

- 一次取色可以完成多笔，下一次动作由任务、笔宽或所需颜色变化触发。不能为了展示而固定“每画 N 笔沾一次”。
- 同一颜色尽量在当前区域、当前阶段复用；相近颜色只有在不会明显损害关键色阶时才能合并。不要为少取色牺牲肤色结构。
- 保留前后覆盖关系。不能把整幅画所有白色一次画完，因为后续头发、衣服、背景覆盖可能要求回到白色修边。
- 第一层试验可以只改任务划分与批次，继续沿用当前实际上色量，不宣称颜料耗尽。若以后批准剩余颜料模型，消耗必须关联真实路径、笔宽和上色量，并影响落笔结果；还需验证换色残留及恢复/重播一致性。
- 保留取笔、到正确色盘沾色、到画布落笔的真实位置关系；动作频率由计划决定，播放速度只控制时间。减少无用远距离移动与工具切换后，才评估动画时长，不能用隐藏沾色来冒充更合理的过程。

减少取色往返有望缩短过程，但局部规划可能增加准备时间，重要细节也可能需要更多笔触。因此分别记录规划时间、落笔计算、取用次数、移动距离与完整播放时间，不提前承诺节省比例。

## 大模型的地位

当前建议先用现有本地路线验证上述问题。未来可以单独比较模型提供区域、重要部位和阶段建议是否有帮助，但模型建议需要校验，也不能保证人物相似度；即使分析正确，有限色盘、路径与笔触覆盖仍可能丢细节。每次落笔继续由确定性代码执行。不在本轮引入模型、上传图片或预建接口。

## 建议的后续验证顺序（尚未实施）

| 顺序 | 范围 | 判断与停止条件 |
| --- | --- | --- |
| A 定位保真损失 | 保留原三图；人物使用有许可样本，或经用户同意仅本地处理其原图。留存分析/颜色/材质对照及关键局部 | 找到可复现的损失环节再改；没有对应原图和计划就不冒充已完成该人物诊断 |
| B 小范围任务规划 | 同一固定输入，在几个明确区域试验任务与颜色批次；复用现有引擎，版本化保存旧/新计划 | 同时检查主要构图、遮挡及取用次数；动作减少但形体变差判失败，不扩大到全应用 |
| C 有针对性的细节修正 | 按 A 的证据逐项试验区域分析、色阶或路径/厚度；每次只改变可解释因素 | 保留全部失败结果；沿用既有性能停止门槛。若主要结构仍不能保留或出现持续卡顿，提交对照和原因后停止扩大功能 |

这些建议不重新开启已结束的两轮修正预算。下一轮实际开发需用户确认具体范围与有限预算，本轮分析不算效果已经改善。

验收应同时看：同输入原图/旧结果/新结果与局部放大；每阶段真实 PNG 和完整录像；取色批次、单笔批次占比、换笔和移动距离；规划与播放耗时、内存和长时间卡顿。保留暂停、速度、重播颜色/高度一致性、导出无参考层、取消不损坏 M2 草稿等回归。新的任务规划可以生成不同画作，但同一个新计划在不同速度下必须一致。

## 本轮验证与未解决事项

- **通过：** 三份历史计划统计；批次数与计划记录一致；阶段笔数与总数一致；切换原因计数一致；读取前后源文件 SHA-256 一致。脚本为本项目新增，无第三方代码、依赖或素材。
- **通过：** `node --check scripts/analyze-painting-tasks.mjs`；重复运行后统计 JSON SHA-256 完全一致；提交前空白检查。以上只验证分析脚本与资料，不证明新画作质量。
- **未测试：** 新方案的成品质量、节省次数、性能改善；尚未实现新方案，也没有生成新的演示或图片。
- **未重新执行：** 应用 typecheck/build、引擎/浏览器与性能回归，因为应用和测试代码未改变。上一轮运行结果仍只引用 `E1_PREPARED_STUDIO_REPORT.md`，不是本轮复测结果。
- 上一轮仍记录了孤立长帧间隔；没有证据表明本分析消除了它。帧间隔不等于输入延迟，自动测试不等于真实用户手感。
- M1 V4、M2 人工完整体验、E1 画作与过程继续保留待用户确认。本轮不合并 main、不部署，不公开私人图片或结果。
