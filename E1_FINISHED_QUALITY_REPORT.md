# 自动成品质量改进

基线 cabf663，分支 codex/e1-finished-quality。新授权与未参与调参的人物照片在 c7824d8 锁定。保留原版、结构优先及 Painting/StudioRenderer，M1 V4、M2 与本轮人工成品/过程仍待用户确认。未合并 main、未部署。

## 第一轮实际修改

- 原始解码图片独立取得 1024 细节参考；512 仍用于全局用色分析，未将 512 放大冒充新细节。
- 维持 24 盘，重新调整有限色盘代表色与红绿差异代价；原始参考不先被色盘替换。每笔仍对应可显示、可校验的实际色盘。
- 对候选笔触的保守覆盖范围，使用真正 Painting 试画，检查整片颜色收益以及对已正确区域的破坏。无效候选同时恢复颜色与高度。固定复用 384 KiB 局部缓冲，不创建全图历史。
- 根据笔宽横向色差缩小笔宽，沿局部方向限制路径，逐轮刷新残差，成功笔数仍不超过 8020。

已复用会话给定的私人原图及历史诊断，在仓库外留下同图第一轮 PNG/五阶段/计划。肤色异常有所减轻，部分眼镜及鼻口线索重现；天空与衣裙仍粗，不能宣称人物已可用。第一轮没有手动圈区参与规划。其可视化脚本还使用了旧色盘诊断图标签，该项不作为第一轮色盘依据；实际笔触颜色以计划为准，后续脚本已改用质量色盘。

第一轮类型检查、三个新增引擎检查：通过。覆盖错误反向测试证明实际拒绝并完整恢复颜色/高度；接受候选与原引擎独立执行完全相同；有限色盘确定且不超过 24。记录在 artifacts/e1/finished-quality/r1-checks。

## 第二轮与策略冻结

第二轮尝试更均匀的底色采样、较轻细笔接触、局部材料分组，降低频繁取色。分组实现中补充了各区域按候选人口分配的上限，防止前序区域耗尽预算；初次执行与修正后的执行都保存在私人目录，未删除。最终第二轮的五官比第一轮更模糊，**未采用**。完整源码保存为 artifacts/e1/finished-quality/r2-quality-source.txt，差异 r2-source.patch 保留原记录，另有统一 LF 的 r2-source-lf.patch 经只读 apply 检查通过；没有将其应用回正式代码。可与 97edf50 中第一轮源码比较；两轮私人计划均独立重放验证了颜色与高度一致。

最终停止调参，选择第一轮 `e1-finished-quality-1` 作可选实验。原版默认与结构优先行为保留；新增“成品细节 · 实验”通过实际 Worker、备料校验和原播放器执行。准备、取笔、沾色、画笔接触与归位仍对应真实笔触，切换不自动替换作品。没有手动圈区、补画入口、隐藏落笔颜色或图片贴图。

## 冻结后独立验证：成品目标未通过

开发前固定的 NASA 人物照片在算法冻结后首次规划，没有用于再次调参。面部、头发和颈圈仍有缺失，第一轮在私人案例中的局部收益没有稳定迁移，**不满足重要可见细节稳定保留的目标**。人物问题不能写成已修好。原固定静物和复杂场景同样存在碎裂、空隙或细节丢失；风景主要构图仍可辨，不能由此宣称整体画质升级成功。

公开对照见 artifacts/e1/finished-quality/index.html：同一输入、contain 构图、1024 显示坐标、seed 1906，包含原图/当前结构优先基线/新策略的全图与同范围局部、五阶段、实际 PNG、序列化计划和测量记录。局部裁框只用于验收显示，不进入算法。私人对照与录像只在仓库外本地入口，不纳入 GitHub 或开发服务。

| 固定样本 | 基线 → 新策略笔数 | 基线 → 新策略取色 | 规划秒（基线/新） | 离线绘画计算秒（基线/新） |
| --- | ---: | ---: | ---: | ---: |
| 独立人物 | 7659 → 8020 | 2774 → 6286 | 5.54 / 3.84 | 4.91 / 2.38 |
| 风景 | 7333 → 8020 | 2103 → 5605 | 5.82 / 4.80 | 5.19 / 2.62 |
| 静物 | 7440 → 8020 | 2485 → 5858 | 4.53 / 4.17 | 4.24 / 2.14 |
| 复杂场景 | 7315 → 8020 | 2316 → 5304 | 4.75 / 4.07 | 4.61 / 2.50 |

以上为本轮顺序执行的离线阶段检查，包含每阶段导出以外的实际引擎调用时间；不是无干扰性能基准。新笔触较短，绘画计算可能减少，但更多取色会增加动作展示时间。材料仍为 24 盘；具体画笔、路径、上色量和固定种子在各计划中，不使用不同样本比较。质量没有通过，RMSE 仅在原始记录中作诊断。

## 功能、录像与资料检查

| 项目 | 状态 | 本轮证据 |
| --- | --- | --- |
| 类型检查、构建 | 通过 | 实际执行 npm run typecheck / npm run build；现有 Pages 工作流未改 |
| 4 个固定样本的真实规划、五阶段 PNG 与状态 | 通过 | frozen/tests-results.json；这里的阶段执行是离线检查，不冒充过程录像 |
| M1/M2/E1 及新策略引擎回归 68 项 | 通过 | regression/tests-results.json，含独立撤销预期、故意错误高度反证、混色、降级、M2 草稿/签名/完整旅程 |
| 新计划四图 × 三速度精确状态 | 通过 | regression/quality-speeds，0.5/1/4× 合成帧时钟；不冒充真实速度录像 |
| 旧结构优先重新规划仍与历史逐字段相同 | 通过 | review/baseline-exact.json；三张原图的当前规划均与历史计划完全相同 |
| E1 浏览器异常与原版/结构优先交互 6 项 | 通过 | browser-regression/tests-results.json：Worker 失败、120 秒期限的加速注入、迟到解码、取消/替换、上下文丢失、备料/取用、切换与原签名草稿保护 |
| 私人原图完整真实应用过程、下载与独立重放 | 通过 | 仓库外 finished-quality-ui-final；公开仅提供无照片/坐标/私有计划的 private-workflow-summary.json |
| 公开入口与媒体链接 | 通过 | final-review/checks.json：54 个链接、图像解码、1440/700/360 视口；不是物理设备测试 |
| 最终页面明确提示画质失败、入口与窄视口 | 通过 | final-checks：只补充提示与证据链接，类型/构建及实际准备、1440/700/360 布局检查通过 |
| 真实录像及实际 PNG 解码 | 通过 | private-media-validation.json：713.4 秒、1440×900 视频，首/中/末段解码，实际 PNG 1024×1024；不声称逐帧人工看完 |
| 无录屏独立播放性能 | 通过（既定计算批次门槛） | performance/continuous.json，具体长帧与耗时见下文；不是零卡顿或成品质量通过 |
| 自动成品重要结构与细节稳定改善 | **未通过** | 私人案例有局部收益，独立人像和部分旧样本有明显失败，不宣称全面改善 |
| M1 V4、M2 与本轮人工成品/过程 | 待用户确认 | 不由自动测试替代 |
| 真机触控、其他浏览器、真人体验 | 未测试 | 当前 Windows Chrome 自动化不足以代表这些项目 |

私人完整操作录制实际执行了选图、备料、取笔/沾色/连续绘画、暂停导出、原图开关、模式切换及取消替换、继续到最后归位、实际 PNG 下载、降级导出、离开取消与返回 M2。实际 PNG 与独立计划的颜色/高度完全一致，原图叠层开关不改变 PNG，原画室数组和已存草稿不变。

录制案例：7984 笔、5734 次取色、24 盘/5 支笔；规划约 7.65 秒，实际笔触计算累计约 3.87 秒，下载等待约 0.35 秒。4× 播放至完成的墙钟时间约 693.37 秒，含验收操作与暂停；非落笔动作消耗约 486.90 秒调度时间，路径绘制消耗约 8.85 秒调度时间。调度时间不是实际 CPU 时间，不能把它与墙钟停顿混为一谈。录屏期间同时有其他回归，性能数值仅诊断；另作无录屏独立测量。

首次录像因开发服务器仍发生模块热更新被主动中断，保留仓库外 finished-quality-ui 的中断资料，不作通过证据。冻结应用后以独立新目录完整重录，11.9 分钟测试通过，引用的是这份完整记录。没有用中断视频冒充完整过程。

本地文件的视频画布读回检查受到浏览器同源限制，改用本机已有 FFmpeg 直接解码实际视频采样帧；未解除浏览器保护、未重制录像。首/中/末帧已解码，另查看了中段实际绘画画面；人工过程体验仍待用户确认。

## 内存范围与停止结论

画作颜色/高度仍为 6 MiB，自动历史为 0，沿用一个 4 MiB 引擎工作缓冲。新策略全局参考 1 MiB，原始输入独立采样的细节参考 4 MiB，局部试画备份固定 384 KiB；没有逐笔整图快照。旧规划器的逐阶段模糊临时数组不在新策略中使用。候选数由 1024 画幅和最小 3 像素网格约束，尝试次数最多每阶段三轮、每轮不超过剩余笔数的四倍。输入 12 MB/1200 万像素/单边 8192、8020 笔、8 支笔/24 盘、120 秒规划期限均未扩大。

五次新策略载图/退出后，页面堆 usedSize 从 6,222,248 到 8,783,028 字节（约 +2.44 MiB），backing storage 从 14,390,570 到 14,393,649 字节；小于原页面堆增长门槛。此读数不包含全部 Worker、GPU 和原生解码内存，不能宣称整机峰值内存已测。最大输入压力、长期整机峰值、低内存设备：未测试。

两轮上限已到，本轮停止继续调参。失败的关键假设是：以全图局部颜色收益排序、固定有限色盘与最小扁刷，便足以保证完整作品和重要结构。实际结果表明局部收益检查能拒绝破坏性候选，却不能保证预算落到关键结构，也不能保证所有必要区域得到足够覆盖；底色接近目标时，颜色代价还可能忽略覆盖不足。下一项需要专项改变和验证的是“关键结构与覆盖完整性必须满足”的规划约束和自动确定其优先级的方式，而非继续增加整幅笔数或让用户补画。未引入模型/服务、未扩大资源上限。

## 独立性能与运行方式

所有其他本轮浏览器和引擎测试结束后，单独运行固定复杂场景 4×；无录屏、截图或测量期导出。原生 1024 画作，实际显示背板 486×486，Windows Chrome / ANGLE Intel UHD / WebGL2。共 8020 笔；含规划墙钟 463.27 秒，规划 3.83 秒，实际笔触计算累计 3.01 秒。动作消耗约 450.81 秒调度时间，落笔路径约 7.95 秒调度时间；两种调度值均不包含浏览器停顿，不能与实际 CPU 时间相加。

27,517 个批次，全程最大 8.0 ms，无连续三批 >100 ms；前 20,000 批的 p95 为 0.20 ms，样本中 >100 ms 为 0，**通过既有批次门槛**。规划期帧间隔 p95 4.3 ms、最大 33.5 ms；播放期 p95 4.3 ms，但有一次 **220.9 ms** 的间隔，未记录对应 Long Task，原因未确认。不能宣称零卡顿，也不能把帧间隔当输入延迟。较低的批次 p95 包含大量无落笔的动作批次，不能解释成人物细节算得更好。

相较上一轮结构优先复杂场景的历史 229.34 秒记录，本轮总等待明显更长；这不是同场环境的成对性能实验，不能据此声称精确速度比。取色 2316 → 5304 的实际计划差异解释了主要动作成本。减少绘画计算、通过批次门槛不代表过程体验合格。

本轮仅改变图片自动策略及观察性计时。M1 手动画笔/渲染、M2 自动保存热路径未改，既有长期手动画笔与自动保存性能仍引用 E1_STRUCTURE_MODE_REPORT.md / E1_BRUSH_PROCESS_REPORT.md 的历史记录，不冒充本轮重测。其他新数据均标明目录和测量范围。

本地服务实际启动日志确认：VITE v8.2.2，http://127.0.0.1:5174/ 。本机使用的启动命令为：

```text
node node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5174 --strictPort
```

画室内进入“图片自动绘制 · 实验”，选择“成品细节 · 实验”。原版仍是默认。页面明确提示人物成品验证未通过，底部提供本轮实测入口。公开对照页：http://127.0.0.1:5174/artifacts/e1/finished-quality/index.html 。该页面只用于验收，不是可绘画应用。

实际执行类型检查、构建，以及 Playwright 的 engine / studio / m2 / e1-engine / e1-quality、受影响 E1 浏览器回归、独立 e1-performance 和仓库外的 e1-quality-ui 完整操作。全部运行使用独立 M1_ARTIFACT_DIR，没有覆盖旧证据或其他运行的视频目录。final-checks 保存最终类型/构建日志。最终仅增加明确的失败提示和证据链接，没有在视频或独立性能完成后再修改绘画策略。

实施已按步骤推送：c7824d8 锁定授权与独立样本，97edf50 第一轮引擎改进，c59336b 接入可选实验及失败的独立画质验证，最终证据另作收尾提交。未合并 main、未修改工作流、未部署。**本轮开发验证结束，自动成品质量目标未通过；按两轮上限停止，等待成品与过程验收及下一项技术假设的决定。**
