# E1 备笔、固定色盘与真实取用过程

本轮实施基于用户确认的 `E1_PREPARATION_LAYOUT_V2.md`。主画室保持原版，只修改独立图片实验。基线为 `7409aac`，开始时本地干净且与远端 E1 分支一致。不合并 main、不部署；M1 V4、M2 与 E1 人工验收继续待用户确认。

## A：实际备料与取用闭环

已实现本地选图 → 确认构图并准备 → 检查固定笔具与颜色 → 明确开始 → 从笔架取笔、到对应色盘沾色、实际绘画 → 暂停与实际 PNG 导出。工具按原版风格集中在右侧，原图独立且可关闭。开始与播放控制放在画布上方，避免准备好后主要入口落到视口下方。

在 Worker 中用原创确定性加权中位切分整理最多 24 盘颜色；透明留白不参与配色。每笔使用真实色盘颜色，规划误差反馈也以实际新颜色绘画，不仅修改显示。现有轮廓笔宽保留，计划按需准备最多 8 支独立规格笔具。草案中的 5 支/12 盘是示意，没有作为成品质感的强制限制。

准备结果记录材料版本、固定笔具/色盘清单以及每笔的 `brushId` / `dishId`。沿用原 1024×1024 Painting / StudioRenderer 和固定采样。屏幕上的笔具与色盘位置映射至独立移动坐标；笔尖实际到对应盘内取色，取出的笔位留空，归位前擦拭。擦拭只清除当前所载颜色，不模拟化学洗笔或颜料耗尽。速度不改变绘画参数或顺序。

新规划版本 `e1-prepared-studio-1` 会改变笔触用色、误差选择及最终结果，不能再声称与上一轮 PNG 相同。历史计划执行行为保留；固定原三图、构图、seed 1906 与画笔版本 2 不变。有限色盘的质量尚需后续固定样本对照。

本增量检查：类型检查、构建、新增 3 项材料/动作引擎测试及 1 项真实浏览器闭环**通过**。材料确定性、有界数量、引用校验、不同速度的颜色/高度一致、暂停及清洁归位已经检查。浏览器验证准备后不自动画、对应盘取色位置、暂停、真实部分画作 PNG、参考不入导出、构图更改使准备失效、取消保留结果与原主画室/草稿隔离。

证据：`artifacts/e1/prepared-studio/a-engine` 与 `a-browser-verified`。`a-browser` 是首次布局检查，开始按钮位于画布下方，随后将其与播放操作移到上方并复测；原记录保留。这里的 PNG 为明确标记的部分画作，完整三图尚未交付，不能替代最终证据。

后续仍需完整固定三图、旧流程适配后的 E1 浏览器回归、M1/M2 回归、性能/内存与最终交付。当前阶段不代表完整验收通过。

## B：接触定位与原流程回归

暂停在沾色时调整窗口尺寸，现在会把笔尖重新对齐至同一固定色盘；颜色、高度和沾色进度不变。已在 1000、700、1440 像素视口验证，视口模拟不代表真机。原流程测试改为先明确确认准备，再执行真实绘制。

有限色盘会改变实际规划结果，因此当前计划的预期值改为独立逐笔执行序列化计划后分别校验颜色与高度 SHA-256；不再错误要求新材料计划与旧无限用色 PNG 相同。旧计划重放与历史三图的精确一致检查继续保留。新旧成品差异独立记录，数值一致不代替视觉质量判断。

本次通过：类型检查、构建；M1/E1 引擎共 18 项；M1/M2 浏览器共 12 项；E1 短流程 5 项，涵盖异常、取消/换图竞态、资源释放、固定色盘、笔头接触与缩放。接触定位改动后又重跑材料引擎 3 项与接触浏览器 1 项，均通过。原始结果分别在 `b-checks`、`b-engine`、`b-regression`、`b-browser`、`b-contact`。

`b-quality` 是三张固定照片的直接引擎分阶段诊断，不是定时播放或录像证据。风景 7046 笔 / 4005 次取色，静物 7305 笔 / 5306 次取色，复杂场景 7240 笔 / 4600 次取色；各有 24 盘颜色和 8 支实际笔具。主要构图仍可辨认，有限颜色造成色差，小文字、咖啡豆等细节仍粗糙。接下来仅验证不相交笔触的局部顺序能否减少往返，并保持全部阶段颜色/高度精确一致；完整播放与独立性能仍待执行。人工验收继续待用户确认。

## 聚焦修正 1：减少局部不必要的取用

已同步 A `a9f0445`、B `4e3a39a`。新材料计划版本改为 `e1-prepared-studio-2`，只对同一 128 像素局部内、不影响任何先前相交笔触的候选重排。最多查看 64 笔，加入换笔/取色的固定启发式成本；这不是实测毫秒或总路程。旧材料未准备的计划维持原排序规则。另在规划期间禁用速度选择，避免准备时修改速度与生成后的播放设置不一致。

相对 B 诊断，风景取色 4005 → 3832，静物 5306 → 5193，复杂场景 4600 → 4485；减少幅度有限，不承诺模拟专业画师的思考。新顺序的三图全部五阶段颜色、高度分别精确相同，未改笔宽、颜色、路径、种子或绘画引擎。检查：6 项材料/顺序/速度引擎测试、1 项准备与沾色浏览器测试、类型检查和构建通过。证据在 `b-ordering`、`b-ordering-browser`、`b-ordering-checks`。停止继续调整用色和规划，下一步录制完整三图与测量性能。

## C：三张固定照片的完整实际播放

`c/tests-results.json`：3 项完整浏览器取证通过，总用时 27.3 分钟。每张都从本地选图和明确开始，经真实取笔/沾色/五阶段落笔，直至擦拭归位、PNG 下载和独立重放核对。全程录像分别约 6.7、11.3、9.3 分钟（测试总时长，实际视频元数据在最终入口检查中另记），播放速度 4×。静物换色更频繁，过程更长，这仍需人工判断。

三张新规划各五阶段颜色/高度与 B 修正前诊断一致；最终颜色/高度也与独立执行序列化计划一致。PNG 1024×1024 可导出，与最终阶段 PNG 相同；移除参考层后 PNG 不变。最后清洁归位不改变作品，没有外部图片请求或页面异常。旧结果对照作为差异记录，未错误标成与新有限色盘相同。

所有固定样本与实际完整视频位于 `c/landscape`、`c/still-life`、`c/complex`；每个目录含五阶段 PNG、实际最终 PNG、输入参数、完整笔触/材料计划、页面、参考关闭后的截图、录像和原始指标。此次录像没有并行其他浏览器或引擎测试。录屏指标只作采集环境诊断，独立性能尚待完成。

## 交付范围与限制（适用于本轮）

- 原主画室、M2 引导、草稿、签名与手动撤销保持原实现。变更集中在 `src/experiment` 和相关测试，新增备料清单、右侧工具布局、实际取用状态及有界局部顺序规则；没有新增依赖。
- 本地照片限制仍为 PNG/JPEG、12 MB、1200 万像素、单边 8192；分析 512×512，落笔和 PNG 为 1024×1024。固定三张有权再分发的照片沿用原输入 SHA-256、contain 构图和 seed 1906；画布 seed 906、画笔版本 2。
- 色盘由整张图的有限颜色量化产生。小面积关键颜色可能被合并，复杂文字、人物、车辆和细枝等精确还原未通过；本轮没有语义识别或专业画师步骤。布局/画作/长期观看体验仍待用户确认。
- 画笔规格与实际笔宽一致，取出的笔位留空；同一载色继续使用，需要换颜色或上色量才再取色。擦拭清除载色状态，未模拟真实颜料余色、耗尽和化学洗笔。
- 自动作品不保存整笔撤销快照；手动画笔最近 20 笔仍由原引擎处理。实验不覆盖 M2 草稿、不上传图片、不承诺刷新恢复，退出前提示导出。
- 真机触控/压感、低配置设备、其他浏览器、最大图片整机压力与长期峰值内存：未测试。页面堆数据不包含全部 Worker、GPU 和浏览器原生缓冲。历史 M1 全套热路径性能、M2 连续绘画/自动保存性能继续引用 `E1_BRUSH_PROCESS_REPORT.md` 的原记录，本轮没有改这些热路径。
