# E1 备笔、固定色盘与真实取用过程

本轮实施基于用户确认的 `E1_PREPARATION_LAYOUT_V2.md`。主画室保持原版，只修改独立图片实验。基线为 `7409aac`，开始时本地干净且与远端 E1 分支一致。不合并 main、不部署；M1 V4、M2 与 E1 人工验收继续待用户确认。

**交付状态：实现与本轮自动化验证已完成；画作、布局和过程待用户确认。** 既定 CPU 批次性能门槛通过，但仍记录到偶发画面调度停顿，未完全定位，不宣称零卡顿。下方 A/B/C 是各增量的当时记录，最终结果以本节及“最终验证与入口”为准。

实际应用：<http://127.0.0.1:5174/>，从「图片自动绘制 · 实验」进入。完整资料：<http://127.0.0.1:5174/artifacts/e1/prepared-studio/index.html>。复用已运行的本地 Vite，已确认 HTTP 200、标题“慢光 · 数字油画室”；重新启动使用 `npm run dev -- --host 127.0.0.1 --port 5174 --strictPort`。本轮未启动新的部署服务，构建日志的 Published 仅指本地 dist 的历史导航生成。

## A：实际备料与取用闭环

已实现本地选图 → 确认构图并准备 → 检查固定笔具与颜色 → 明确开始 → 从笔架取笔、到对应色盘沾色、实际绘画 → 暂停与实际 PNG 导出。工具按原版风格集中在右侧，原图独立且可关闭。开始与播放控制放在画布上方，避免准备好后主要入口落到视口下方。

在 Worker 中用原创确定性加权中位切分整理最多 24 盘颜色；透明留白不参与配色。每笔使用真实色盘颜色，规划误差反馈也以实际新颜色绘画，不仅修改显示。现有轮廓笔宽保留，计划按需准备最多 8 支独立规格笔具。草案中的 5 支/12 盘是示意，没有作为成品质感的强制限制。

准备结果记录材料版本、固定笔具/色盘清单以及每笔的 `brushId` / `dishId`。沿用原 1024×1024 Painting / StudioRenderer 和固定采样。屏幕上的笔具与色盘位置映射至独立移动坐标；笔尖实际到对应盘内取色，取出的笔位留空，归位前擦拭。擦拭只清除当前所载颜色，不模拟化学洗笔或颜料耗尽。速度不改变绘画参数或顺序。

新规划版本 `e1-prepared-studio-1` 会改变笔触用色、误差选择及最终结果，不能再声称与上一轮 PNG 相同。历史计划执行行为保留；固定原三图、构图、seed 1906 与画笔版本 2 不变。有限色盘的质量尚需后续固定样本对照。

本增量检查：类型检查、构建、新增 3 项材料/动作引擎测试及 1 项真实浏览器闭环**通过**。材料确定性、有界数量、引用校验、不同速度的颜色/高度一致、暂停及清洁归位已经检查。浏览器验证准备后不自动画、对应盘取色位置、暂停、真实部分画作 PNG、参考不入导出、构图更改使准备失效、取消保留结果与原主画室/草稿隔离。

证据：`artifacts/e1/prepared-studio/a-engine` 与 `a-browser-verified`。`a-browser` 是首次布局检查，开始按钮位于画布下方，随后将其与播放操作移到上方并复测；原记录保留。这里的 PNG 为明确标记的部分画作，完整三图尚未交付，不能替代最终证据。

后续仍需完整固定三图、旧流程适配后的 E1 浏览器回归、M1/M2 回归、性能/内存与最终交付。当前阶段不代表完整验收通过。

## B：接触定位与原流程回归

暂停在沾色时调整窗口尺寸，现在会把笔尖重新对齐至同一固定色盘；颜色、高度和沾色进度不变。已在 1000、700、1440 像素视口验证，视口模拟不代表真机。原流程测试改为先明确确认准备，再执行真实绘制。

有限色盘会改变实际规划结果，因此当前计划的预期值改为独立逐笔执行序列化计划后分别校验颜色与高度 SHA-256；不再错误要求新材料计划与旧无限用色 PNG 相同。旧计划重放与历史三图的精确一致检查继续保留。新旧成品差异独立记录，数值一致不代替视觉质量判断。

本次通过：类型检查、构建；M1/E1 引擎共 18 项；M1/M2 浏览器共 12 项；E1 短流程 5 项，涵盖异常、取消/换图竞态、资源释放、固定色盘、笔头接触与缩放。接触定位改动后又重跑材料引擎 3 项与接触浏览器 1 项，均通过。原始结果分别在 `b-checks`、`b-engine`、`b-regression`、`b-browser`、`b-contact`。

`b-quality` 是三张固定照片的直接引擎分阶段诊断，不是定时播放或录像证据。风景 7046 笔 / 4005 次取色，静物 7305 笔 / 5306 次取色，复杂场景 7240 笔 / 4600 次取色；各有 24 盘颜色和 8 支实际笔具。主要构图仍可辨认，有限颜色造成色差，小文字、咖啡豆等细节仍粗糙。接下来仅验证不相交笔触的局部顺序能否减少往返，并保持全部阶段颜色/高度精确一致；完整播放与独立性能仍待执行。人工验收继续待用户确认。

## 聚焦修正 1：减少局部不必要的取用

已同步 A `a9f0445`、B `4e3a39a`。新材料计划版本改为 `e1-prepared-studio-2`，只对同一 128 像素局部内、不影响任何先前相交笔触的候选重排。最多查看 64 笔，加入换笔/取色的固定启发式成本；这不是实测毫秒或总路程。旧材料未准备的计划维持原排序规则。另在规划期间禁用速度选择，避免准备时修改速度与生成后的播放设置不一致。

相对 B 诊断，风景取色 4005 → 3832，静物 5306 → 5193，复杂场景 4600 → 4485；减少幅度有限，不承诺模拟专业画师的思考。新顺序的三图全部五阶段颜色、高度分别精确相同，未改笔宽、颜色、路径、种子或绘画引擎。检查：6 项材料/顺序/速度引擎测试、1 项准备与沾色浏览器测试、类型检查和构建通过。证据在 `b-ordering`、`b-ordering-browser`、`b-ordering-checks`。停止继续调整用色和规划，下一步录制完整三图与测量性能。

## C：三张固定照片的完整实际播放

`c/tests-results.json`：3 项完整浏览器取证通过，总用时 27.3 分钟。每张都从本地选图和明确开始，经真实取笔/沾色/五阶段落笔，直至擦拭归位、PNG 下载和独立重放核对。全程录像分别约 6.7、11.3、9.3 分钟（测试总时长，实际视频元数据在最终入口检查中另记），播放速度 4×。静物换色更频繁，过程更长，这仍需人工判断。

三张新规划各五阶段颜色/高度与 B 修正前诊断一致；最终颜色/高度也与独立执行序列化计划一致。PNG 1024×1024 可导出，与最终阶段 PNG 相同；移除参考层后 PNG 不变。最后清洁归位不改变作品，没有外部图片请求或页面异常。旧结果对照作为差异记录，未错误标成与新有限色盘相同。

所有固定样本与实际完整视频位于 `c/landscape`、`c/still-life`、`c/complex`；每个目录含五阶段 PNG、实际最终 PNG、输入参数、完整笔触/材料计划、页面、参考关闭后的截图、录像和原始指标。此次录像没有并行其他浏览器或引擎测试。录屏指标只作采集环境诊断，独立性能尚待完成。

本轮完整速度检查在 `c-engine`：30 项全部通过，包括 M1 引擎 7 项、历史 E1 引擎 8 项、新材料/顺序/速度 15 项。三张完整真实计划分别以 0.5×、1×、4× 在确定的测试 rAF 时钟下执行，全部颜色/高度与独立预期相同，笔具清洁归位，撤销历史为空。测试使用真实 Painting 与 PlanPlayer，未改变路径、颜色、顺序或减少笔数；测试时钟结果不是实时录像或 UI 性能。新完整实际录像统一使用 4×。

保留原 E1-A、E1-B 和完整降级续画测试，本轮不重复其长程录像：完整绘制闭环由三张固定图取证覆盖；速度不变性由三图九次完整调度测试覆盖；页面重播/暂停/导出/降级续画与 M2 签名隔离另做首阶段实际操作。历史完整 1× UI 录像继续保留在 `artifacts/e1/brush-process`，不把它冒充本轮新材料计划的 1× 录像。

`c-controls`：新增实际页面回归通过（1.7 分钟）。固定静物首阶段在 4× 完成后取消重播能保留作品；确认从空白重播，以 1× 起笔，暂停在半笔中导出、触发 WebGL 丢失，再切换 4× 续画至同阶段，颜色/高度与首次精确相同。更换图片/退出取消不丢画；真正退出后日落步骤、原签名、颜色/高度、草稿校验与撤销数不变。此录像明确是首阶段操作，不是完整成品或完整 1× 录像。类型检查和构建在 `c-checks` 再次通过。

## 聚焦修正 2：工具栏只接收显示所需状态

首次独立性能 `performance/continuous.json` 通过原批次门槛：7240 笔、4×、总用时 414.19 s、规划 5.01 s；24251 个批次，前 20000 批次 p95 1.40 ms，全部批次最大 17.40 ms，无连续 >100 ms 批次。但播放分类中出现 2183.4 ms 和 429.1 ms 两次帧间隔；第一处紧接规划结果到达，对应 2183 ms 主线程长任务，第二处没有对应 JS 长任务。不能将这轮写成“零卡顿”。

限定启动诊断 `startup-profile` 在 React 开发模式的 `addObjectToProperties` / `addValueToProperties` 中观察到大量属性展开及 GC；工具栏原先接收整个 PlanPlayer，包含 Painting 的大数组。只把界面需要的笔号、色盘号和动作传给 MaterialBoard，材料清单仍是小对象。未修改绘画、渲染、规划或动作调度；不是改引擎或隐藏测试数据。

相同固定复杂图的诊断复测 `startup-profile-r2`，分析器期间仅记录 86 ms、81 ms 两次长任务；原带分析器的 4842 ms 长任务未复现。诊断受分析器影响，不当作独立性能。第二轮修正到此结束，随后复测对应页面与无分析器全程性能；历史原始数据和三图录像保留。

`r2-checks` 类型检查和构建通过；`r2-browser` 的准备/固定盘接触/窗口缩放、笔头与取色参数、重播取消/半笔导出/降级续画/原签名草稿隔离共 3 项通过（2.6 分钟）。无分析器全程性能另存 `performance-r2`，完成前不将初次性能记录替换为新结果。

三图完整录像取自本次工具栏属性补正之前，落笔/材料/计划/最终 PNG 与补正后相同；最新准备与操作录像会在最终入口优先展示。原长录像里的启动停顿不应描述为补正后实测表现。

## 最终验证与入口

| 项目 | 结果 | 实际证据与范围 |
| --- | --- | --- |
| 类型检查、构建 | 通过 | `r2-checks`，包含应用与测试类型；未新增依赖 |
| M1/E1 引擎 | 通过 | `c-engine` 30 项；独立撤销预期及反向验证保留；三图完整计划分别执行三种速度 |
| M1/M2 页面回归 | 通过 | `b-regression` 12 项；草稿、引导、签名、导出、撤销、降级等；后续未改这些模块 |
| E1 异常与资源释放 | 通过 | `b-browser`、`b-contact`；取消/更换/退出、坏图与大小限制、过期任务、五次载图退出 |
| 三图完整页面取证 | 通过 | `c` 3 项；全部五阶段、独立计划重放、PNG、视频；均为 4× |
| 最新准备、接触、重播与降级 | 通过 | `r2-browser` 3 项；实际首阶段操作，不称完整 1× 录像 |
| 独立 CPU 批次门槛 | 通过 | `performance-r2`：p95 ≤50 ms，未出现 3 个连续 >100 ms 批次；未把 rAF 当输入延迟 |
| 全程视觉无停顿 | 未通过 | 本次记录到 5 个 >100 ms 调度间隔，最大 1000 ms，原因未确认；不是另设对外承诺 |
| 资料入口 | 通过 | `review-final`：49 个链接、图片解码、5 段录像元数据及首/中/末帧解码；1440/700/360 视口无横向溢出；保留早先 review、review-r2 |
| 画作质量、真实手感与长期观看体验 | 待用户确认 | 未以数值一致或自动化通过代替人工评价 |

最终独立复杂图 4×：7240 笔，规划 5.66 s，总时长 420.05 s（约 7.00 分钟）。23289 个批次，前 20000 批次 p95 3.00 ms，全部批次最大 11.80 ms，连续 >100 ms 为 0。播放期 rAF 间隔 p95 12.40 ms，最大 1000 ms；5 次 >100 ms 发生在约 17.7、17.9、18.0、20.3、117.8 秒。记录均为页面可见且聚焦，没有对应主线程长任务；只确认原启动长任务在这次复测未再出现，不能把其他间隔归因于已证明的原因，不能宣称整体绘画计算加速。原始首次测量和诊断均未覆盖。

环境：Windows / Node 22.15 / Chrome 152，1440×900，Intel UHD / ANGLE D3D11 / WebGL2，最终实测展示背板 486×486，作品与导出始终 1024×1024。页面五次载图/退出的 heap usedSize 从 6,137,268 到 8,886,304 字节，增长约 2.62 MiB，低于原 <24 MiB 检查；这是 `b-browser/lifecycle` 记录，后续只减少了 UI 传参，不将它称为整机或最终长期峰值内存验证。

最终入口的视频时长：最新准备 17.36 s，风景 395.76 s，静物 676.68 s，复杂场景 552.76 s，最新首阶段重播/降级 123.80 s。全部是实际浏览器操作；已验证元数据、首/中/末帧可解码，没有逐帧审美验收。三图长录像保留性能属性补正前的完整过程，最新准备及控制录像来自补正后的 `r2-browser`。所有原图和实际 PNG 均有可打开入口，不提交私人图片或结果。

已分别同步 A `a9f0445`、B `4e3a39a`、局部顺序 `4c79c39`、完整三图 `8a3de9a`、UI 属性补正与回归 `25b7a96`。最终资料和检查另作收尾提交，推送同一 E1 分支。两轮聚焦修正已结束；停止本轮开发，等待人工验收，不合并 main、不部署、不进入后续图片语义或多风格扩展。

## 范围与未测试项目

- 原主画室、M2 引导、草稿、签名与手动撤销保持原实现。变更集中在 `src/experiment` 和相关测试，新增备料清单、右侧工具布局、实际取用状态及有界局部顺序规则；没有新增依赖。
- 本地照片限制仍为 PNG/JPEG、12 MB、1200 万像素、单边 8192；分析 512×512，落笔和 PNG 为 1024×1024。固定三张有权再分发的照片沿用原输入 SHA-256、contain 构图和 seed 1906；画布 seed 906、画笔版本 2。
- 色盘由整张图的有限颜色量化产生。小面积关键颜色可能被合并，复杂文字、人物、车辆和细枝等精确还原未通过；本轮没有语义识别或专业画师步骤。布局/画作/长期观看体验仍待用户确认。
- 画笔规格与实际笔宽一致，取出的笔位留空；同一载色继续使用，需要换颜色或上色量才再取色。擦拭清除载色状态，未模拟真实颜料余色、耗尽和化学洗笔。
- 自动作品不保存整笔撤销快照；手动画笔最近 20 笔仍由原引擎处理。实验不覆盖 M2 草稿、不上传图片、不承诺刷新恢复，退出前提示导出。
- 真机触控/压感、低配置设备、其他浏览器、最大图片整机压力与长期峰值内存：未测试。页面堆数据不包含全部 Worker、GPU 和浏览器原生缓冲。历史 M1 全套热路径性能、M2 连续绘画/自动保存性能继续引用 `E1_BRUSH_PROCESS_REPORT.md` 的原记录，本轮没有改这些热路径。
