import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { resolve } from 'node:path';
const root = 'artifacts/e1/brush-process';
const names = { landscape: '黄石冬景', 'still-life': '咖啡与白瓷杯', complex: '复杂夜景' };
const notes = { landscape: '树群、雪地和前景的既有概括保持不变；细枝和栅栏仍不精确。', 'still-life': '杯口、杯碟与咖啡豆的既有结果保持不变；豆粒细部与白瓷纹理仍需人工判断。', complex: '街道与灯光布局保持不变。文字、车辆、人物等细节仍明显失真，精确还原未通过。' };
const cards = Object.entries(names).map(([sample, name]) => {
  const samplePath = sample === 'still-life' ? 'c-recording-retry/still-life' : `c/${sample}`;
  const r = JSON.parse(readFileSync(`${root}/${samplePath}/results.json`, 'utf8'));
  const plan = JSON.parse(readFileSync(`${root}/${samplePath}/plan.json`, 'utf8'));
  return `<section><h2>${name}</h2><div class="compare">${[
    [`../fixtures/${sample}.jpg`, '固定原图'], [`../refinement/c/${sample}/final.png`, '上一版成品'], [`${samplePath}/final.png`, '本轮实际 PNG'],
  ].map(([path, label]) => `<figure><a href="${path}"><img src="${path}" alt="${name} · ${label}"></a><figcaption>${label}</figcaption></figure>`).join('')}</div><p>${notes[sample]}</p><p class="details">${r.strokeCount} 笔 / ${plan.pickups.length} 次沾色 · 规划 ${(r.planningMs / 1000).toFixed(2)} 秒 · ${r.speed}× 录像，含操作暂停 · 批次 p95 ${r.p95BatchMs.toFixed(2)} ms<br>画布内笔间路程减少 ${(100 * (1 - r.processMetrics.travel / r.processMetrics.previousTravel)).toFixed(1)}%；这不包含新增往返沾色台的路程。颜色与高度分别与上一版精确一致。</p><div class="stages">${plan.stages.map((s, i) => `<a href="${samplePath}/stage-${i + 1}.png"><img loading="lazy" src="${samplePath}/stage-${i + 1}.png" alt="${name} · ${s.name}"><span>${s.name}</span></a>`).join('')}</div><details><summary>查看从空白到导出的真实录像</summary><video controls preload="metadata" src="${samplePath}/process.webm"></video></details><nav><a href="${samplePath}/final.png" download>实际 PNG</a><a href="${samplePath}/plan.json">笔触与取色计划</a><a href="${samplePath}/input.json">输入参数</a><a href="${samplePath}/results.json">测试原始数据</a><a href="${samplePath}/reference-hidden.png">隐藏参考后的页面</a></nav></section>`;
}).join('');
const performanceLink = existsSync(`${root}/performance/continuous.json`) ? '<a href="performance/continuous.json">完整连续性能</a>' : '';
const html = `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>慢光 · 笔头与沾色过程验收</title><style>
body{margin:0;background:#f1ecdf;color:#514735;font:14px/1.8 "Microsoft YaHei",sans-serif}main{max-width:1220px;margin:auto;padding:36px}h1,h2{font-family:KaiTi,serif;font-weight:400}h1{font-size:34px}h2{font-size:25px}a{color:#536544;text-underline-offset:3px}nav{display:flex;flex-wrap:wrap;gap:18px;margin:18px 0}.button{background:#657154;padding:8px 17px;color:#fff8e8;text-decoration:none}.note{background:#e5ddcb;border-left:3px solid #98886c;padding:15px 20px}section{background:#faf7f0;border:1px solid #d0c4ab;margin:28px 0;padding:24px}.compare{display:grid;grid-template-columns:repeat(3,1fr);gap:15px}figure{margin:0}img{display:block;width:100%;object-fit:contain;aspect-ratio:1;background:#eee8d9}figcaption,.details{font-size:12px;color:#7b705a}.stages{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin:20px 0}.stages span{font-size:11px}details{margin:18px 0}summary{cursor:pointer;color:#62704c}video{display:block;width:100%;margin-top:14px;background:#dfd5c0}.process{display:grid;grid-template-columns:1fr 1fr;gap:18px}.process img{aspect-ratio:auto}footer{color:#867861;font-size:12px}@media(max-width:700px){main{padding:18px}section{padding:15px}.compare,.process{grid-template-columns:1fr}.stages{grid-template-columns:repeat(2,1fr)}}
</style></head><body><main><p>SLOWLIGHT / BRUSH & PAINT</p><h1>看得到笔头，也看得到沾色。</h1><p>当前改进是绘制过程：扁刷粗细与方向对应真实接触，沾色参数用于实际落笔，局部不相交的笔触按距离和相近颜色调整顺序。没有增加新风格、颜料耗尽或画师语义推理。</p><nav><a class="button" href="../../../" target="_blank" rel="noopener">进入慢光画室 ↗</a><a href="../../../E1_BRUSH_PROCESS_REPORT.md">完整报告</a><a href="../refinement/index.html">上一轮 E1 细节</a><a href="../../m1/index.html">M1 验收</a><a href="../../m2/index.html">M2 体验</a><a href="../../../SOURCES.md">照片来源与许可</a></nav><div class="note"><strong>画作与过程：待用户确认。</strong>本轮成品数据与上一版相同，不能把新的笔头当作画质提升证据。保留主要构图的现有能力和复杂细节的现有限制。M1 V4 与 M2 人工验收状态不变。</div><section><h2>沾色、落笔、暂停</h2><div class="process"><a href="c/dipping/dip-paused.png"><img src="c/dipping/dip-paused.png" alt="沾色过程中暂停"></a><a href="c/dipping/real-contact.png"><img src="c/dipping/real-contact.png" alt="真实画布接触与笔头"></a></div><p>用 0.5× 观察笔头；用 4× 较快完成。取色是目标颜色的准备，不是颜料化学或调色配方模拟。为保留原成品，没有合并近似色，因此往返取色仍较频繁。</p><details><summary>查看慢速沾色、暂停和导出录像</summary><video controls preload="metadata" src="c/dipping/process.webm"></video></details><nav><a href="c/dipping/results.json">参数及隔离验证</a><a href="c/dipping/partial.png">真实部分作品 PNG</a>${performanceLink}</nav></section>${cards}<section><h2>同一计划，4× 与 1× 完整重播</h2><p>包含完整实际绘制、暂停与导出、重新播放，以及退出后保留原旅行日落、签名和草稿的操作。1× 段较长，可在播放器中选择查看。</p><details><summary>查看完整正常速度重播录像</summary><video controls preload="metadata" src="c/b-controls-process.webm"></video></details><a href="c/b-invariants.json">速度与草稿隔离检查</a></section><footer>所有照片均沿用有授权的固定样本，未使用私人图片。新资料只在 E1 分支与本机，线上仍为 M2。真实触控笔、低配置设备和人工观感未测试；实验退出前请导出。</footer></main></body></html>`;
for (const match of html.matchAll(/(?:href|src)="([^"]+)"/g)) {
  const target = match[1]; if (!existsSync(resolve(root, target))) throw new Error(`Missing evidence: ${target}`);
}
writeFileSync(`${root}/index.html`, html);
console.log('Brush process gallery built with verified local targets.');
