import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { resolve } from 'node:path';
const root = 'artifacts/e1/refinement';
const samples = [
  { id: 'landscape', name: '黄石冬景', credit: 'NPS / Jim Peaco · 美国公共领域', note: '留白不再布满刷痕，树群和前景边缘细节增加。细树枝、栅栏与雪地层次仍被概括，天空仍有水平笔痕。' },
  { id: 'still-life', name: '咖啡与白瓷杯', credit: 'mcfoodie · CC0 1.0', note: '杯口和杯碟的重划痕减弱，豆粒区域出现更多细笔。白瓷仍有纹路，豆粒的小结构尚不准确。' },
  { id: 'complex', name: '夜间街景', credit: 'Chris Spielmann / National Cancer Institute · 公共领域', note: '保留街道、建筑和灯光的主要布局。车辆、人物、文字和细线仍明显失真，不能当作精细还原已经解决。' },
];
const cards = samples.map(s => {
  const r = JSON.parse(readFileSync(`${root}/c/${s.id}/results.json`, 'utf8'));
  const old = JSON.parse(readFileSync(`artifacts/e1/c/${s.id}/results.json`, 'utf8'));
  const stageNames = JSON.parse(readFileSync(`${root}/c/${s.id}/plan.json`, 'utf8')).stages.map(x => x.name);
  return `<section id="${s.id}"><p class="eyebrow">同一张照片 · 同一随机种子</p><h2>${s.name}</h2><p class="credit">${s.credit} · <a href="../../../SOURCES.md">来源与许可</a></p><div class="compare">${[
    [`../fixtures/${s.id}.jpg`, '固定输入照片'], [`../c/${s.id}/final.png`, `此前 E1 · ${old.strokeCount} 笔`], [`c/${s.id}/final.png`, `本轮实际 PNG · ${r.strokeCount} 笔`],
  ].map(([path, label]) => `<figure><a href="${path}"><img src="${path}" alt="${s.name} · ${label}"></a><figcaption>${label} · 点击打开原文件</figcaption></figure>`).join('')}</div><p class="note">${s.note}</p><p class="metrics">本地规划 ${(r.planningMs / 1000).toFixed(1)} 秒 · 绘制批次 p95 ${r.p95BatchMs.toFixed(1)} ms · 原生 1024 × 1024 · 颜色和高度独立重放一致</p><details><summary>看五个阶段与真实逐笔过程</summary><div class="stages">${stageNames.map((label, i) => `<a href="c/${s.id}/stage-${i + 1}.png"><img loading="lazy" src="c/${s.id}/stage-${i + 1}.png" alt="${label}"><span>${i + 1} / ${label}</span></a>`).join('')}</div><p>原速浏览器录像，含规划、笔尖落笔、阶段暂停和实际导出。图像不是预制成品揭幕；细小笔触在较快速度下可在一帧内完成。</p><video controls preload="metadata" src="c/${s.id}/process.webm"></video></details><nav><a href="c/${s.id}/final.png" download>下载实际 PNG</a><a href="c/${s.id}/reference-hidden.png">关闭参考后的页面</a><a href="c/${s.id}/input.json">输入参数</a><a href="c/${s.id}/plan.json">完整笔触计划</a><a href="c/${s.id}/results.json">原始测试结果</a><a href="r1-verified/${s.id}/final.png">第一阶段细节结果</a></nav></section>`;
}).join('');
const html = `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>慢光 · 细节与笔尖过程对照</title><style>
*{box-sizing:border-box}body{margin:0;background:#ede9e0;color:#4b4538;font:14px/1.9 "Microsoft YaHei",sans-serif}main{max-width:1240px;margin:auto;padding:42px 32px 70px}h1,h2{font-family:KaiTi,serif;font-weight:400;line-height:1.35}h1{font-size:36px}h2{font-size:27px}.eyebrow{font:11px Georgia,serif;letter-spacing:2px;color:#8c806d}a{color:#596b48;text-underline-offset:4px}nav{display:flex;gap:18px;flex-wrap:wrap;margin:22px 0}.button{background:#667158;color:#fff9ee;padding:9px 19px;text-decoration:none;border-radius:4px}.pending,.note{background:#e7e1d2;padding:14px 20px;border-left:3px solid #9c8e69}.pending{margin:28px 0}section{margin-top:32px;padding:27px;background:#faf8f1;border:1px solid #d1c7b3}.compare{display:grid;grid-template-columns:repeat(3,1fr);gap:17px}figure{margin:0}img{display:block;width:100%;aspect-ratio:1;object-fit:contain;background:#f1edde;border:1px solid #d7cfbd}figcaption,.credit{font-size:11px;color:#8c7e66}.metrics{font-size:12px;color:#63714f}details{border-top:1px solid #d9d0be;padding-top:15px}summary{cursor:pointer;color:#5c6b4b}details p{font-size:12px;color:#8d7e64}.stages{display:grid;grid-template-columns:repeat(5,1fr);gap:13px;margin-top:20px}.stages a{font-size:11px;text-decoration:none}video{display:block;width:100%;background:#e4ddcd}.pen{max-width:950px;margin:auto}.pen img{aspect-ratio:auto}footer{font-size:12px;color:#8b7b64;margin-top:35px}@media(max-width:700px){main{padding:24px 15px}section{padding:16px}.compare{grid-template-columns:1fr}.stages{grid-template-columns:repeat(2,1fr)}h1{font-size:28px}}
</style></head><body><main><p class="eyebrow">SLOWLIGHT / E1 · DETAIL & MOTION</p><h1>同样的照片，更细的笔触。</h1><p>这是新旧结果与真实过程的验收入口。要亲自选图，请进入画室，点击「图片自动绘制 · 实验」。自由绘画和旅行日落继续保留。</p><nav><a class="button" href="../../../" target="_blank" rel="noopener">进入慢光画室 ↗</a><a href="../../../E1_REFINEMENT_REPORT.md">本轮报告</a><a href="../index.html">此前 E1 验收</a><a href="../../m1/index.html">M1 历史</a><a href="../../m2/index.html">M2 完整体验</a></nav><div class="pending">画作与过程质量：<strong>待用户确认</strong>。本轮完成细节修正、可见笔尖和一轮局部显示/性能验证，没有新增风格、模拟画师思考或开启图片 AI 服务。M1 V4 与 M2 人工验收状态不变。</div><p>过程按局部颜色与轮廓，由大色块到小细节规划，再让一支笔沿真实路径逐点绘制。它尚未理解「树」「杯子」等对象结构；算法顺序不等于专业画师的创作步骤。</p><p><a href="manifest.json">固定照片指纹与新参数</a> · <a href="comparison.json">新旧状态及笔尖播放一致性检查</a> · <a href="performance-verified/continuous.json">连续运行性能记录</a></p>${cards}<section><h2>暂停在一笔中间，再接着画</h2><p>笔尖显示实际执行位置；暂停后颜色和高度不动。导出当前进度不结束这笔，继续后最终结果保持一致。笔尖与参考图均不进入 PNG。</p><div class="pen"><a href="c/pen/paused-inside-stroke.png"><img loading="lazy" src="c/pen/paused-inside-stroke.png" alt="真实笔内暂停"></a><details><summary>查看暂停、导出、降级与继续操作录像</summary><video controls preload="metadata" src="c/pen/process.webm"></video></details></div><nav><a href="c/pen/results.json">笔尖与暂停验证</a><a href="c/pen/partial.png">部分画作 PNG</a><a href="c/pen/without-pen.png">隐藏笔尖 PNG</a><a href="c/lifecycle/results.json">异常与资源释放</a><a href="c/errors/results.json">取消与草稿隔离</a><a href="regression/test-m2-results.json">M2 回归</a></nav></section><footer>图片只在本机处理。请在退出实验或刷新前导出；当前实验没有自动保存。桌面 Chrome 自动化已运行，真人画作与过程验收、真机触屏和低配置设备仍未测试。三张固定照片与历史结果全部保留。</footer></main></body></html>`;
for (const match of html.matchAll(/(?:href|src)="([^"]+)"/g)) {
  const target = match[1]; if (target === '../../../' || target.startsWith('#')) continue;
  if (!existsSync(resolve(root, target))) throw new Error(`Missing evidence: ${target}`);
}
writeFileSync(`${root}/index.html`, html);
console.log('Refinement gallery created; all local evidence targets exist.');
