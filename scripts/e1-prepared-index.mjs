import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { resolve } from 'node:path';
const root='artifacts/e1/prepared-studio';
const read=path=>JSON.parse(readFileSync(`${root}/${path}`,'utf8'));
const names={landscape:'黄石冬景','still-life':'咖啡与白瓷杯',complex:'复杂夜景'};
const notes={landscape:'雪地、树群和前景仍可辨认。有限色盘改变了局部色调，细枝与栅栏不精确。','still-life':'杯口、杯碟和桌面保留主要关系。咖啡豆与细小纹理仍粗糙。',complex:'保留街道和灯光的大体布局，文字、车辆与人物细节仍明显失真。精确还原未通过。'};
const figure=(path,label)=>`<figure><a href="${path}"><img src="${path}" alt="${label}"></a><figcaption>${label}</figcaption></figure>`;
const video=(path,label)=>`<details><summary>${label}</summary><video controls preload="metadata" src="${path}"></video></details>`;
const cards=Object.entries(names).map(([sample,name])=>{
  const r=read(`c/${sample}/results.json`),p=read(`c/${sample}/plan.json`);
  return `<section><h2>${name}</h2><div class="compare">${figure(`../fixtures/${sample}.jpg`,'固定原图')}${figure(`../refinement/c/${sample}/final.png`,'历史实际结果 · 未用固定色盘')}${figure(`c/${sample}/final.png`,'本轮实际 PNG · 固定色盘')}</div><p>${notes[sample]}</p><p class="details">${r.strokeCount} 笔 · ${p.materials.brushes.length} 支笔 · ${p.materials.dishes.length} 盘颜色 · ${p.pickups.length} 次取色<br>规划 ${(r.planningMs/1000).toFixed(2)} 秒 · ${r.speed}× 实际播放，录像含阶段暂停和导出 · 录屏期间批次 p95 ${r.p95BatchMs.toFixed(2)} ms</p><div class="stages">${p.stages.map((s,i)=>`<a href="c/${sample}/stage-${i+1}.png"><img loading="lazy" src="c/${sample}/stage-${i+1}.png" alt="${name} · ${s.name}"><span>${s.name}</span></a>`).join('')}</div>${video(`c/${sample}/process.webm`,'展开：从空白、取笔沾色到成品与归位的真实录像')}<nav><a href="c/${sample}/final.png" download>实际 PNG</a><a href="c/${sample}/plan.json">笔触与材料计划</a><a href="c/${sample}/input.json">输入参数</a><a href="c/${sample}/results.json">完整检查数据</a><a href="c/${sample}/reference-hidden.png">原图关闭后的页面</a></nav></section>`;
}).join('');
const perf=read('performance-r2/continuous.json');
const html=`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>慢光 · 备笔与固定色盘验收</title><style>
body{margin:0;background:#f1ecdf;color:#514735;font:14px/1.8 "Microsoft YaHei",sans-serif}main{max-width:1220px;margin:auto;padding:36px}h1,h2{font-family:KaiTi,serif;font-weight:400}h1{font-size:34px}h2{font-size:25px}a{color:#536544;text-underline-offset:3px}nav{display:flex;flex-wrap:wrap;gap:18px;margin:18px 0}.button{background:#657154;padding:8px 17px;color:#fff8e8;text-decoration:none}.note{background:#e5ddcb;border-left:3px solid #98886c;padding:15px 20px}section{background:#faf7f0;border:1px solid #d0c4ab;margin:28px 0;padding:24px}.compare{display:grid;grid-template-columns:repeat(3,1fr);gap:15px}figure{margin:0}img{display:block;width:100%;object-fit:contain;aspect-ratio:1;background:#eee8d9}figcaption,.details{font-size:12px;color:#7b705a}.stages{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin:20px 0}.stages span{font-size:11px}details{margin:18px 0}summary{cursor:pointer;color:#62704c}video{display:block;width:100%;margin-top:14px;background:#dfd5c0}.process{display:grid;grid-template-columns:1fr 1fr;gap:18px}.process img{aspect-ratio:auto}footer{color:#867861;font-size:12px}@media(max-width:700px){main{padding:18px}section{padding:15px}.compare,.process{grid-template-columns:1fr}.stages{grid-template-columns:repeat(2,1fr)}}
</style></head><body><main><p>SLOWLIGHT / PREPARED STUDIO</p><h1>先备好笔与颜色，再开始一幅画。</h1><p>保留原主画室。独立图片实验采用左画布、右木质色盘和笔架；准备好的工具有固定身份，实际取用的颜色用于真实落笔。</p><nav><a class="button" href="../../../" target="_blank" rel="noopener">进入慢光画室 ↗</a><a href="../../../E1_PREPARED_STUDIO_REPORT.md">本轮报告</a><a href="../brush-process/index.html">历史笔头演示</a><a href="../../m1/index.html">M1 验收</a><a href="../../m2/index.html">M2 体验</a><a href="../../../SOURCES.md">图片来源与许可</a></nav><div class="note"><strong>画作、布局和过程：待用户确认。</strong>本轮有限色盘会改变实际用色；与历史成品不相同。局部排序修正前后的五阶段颜色、高度精确一致，但这不能代替视觉判断。M1 V4 与 M2 人工验收状态保持不变。</div><section><h2>准备、取笔与对应色盘</h2><div class="process">${figure('r2-browser/prepared/prepared-screen.png','准备完成，尚未开始')}${figure('r2-browser/prepared/dip-at-fixed-dish.png','笔尖到实际对应色盘内')}</div><p>每张固定图准备 8 支实际规格笔、24 盘颜色。确认后才开始；取出时笔位留空，换色前擦拭，换笔和完成时归位。擦拭是载色状态清除，不是颜料化学或耗尽模拟。参考图、笔具和工具栏不进入 PNG。</p>${video('r2-browser/prepared/actual-preparation-process.webm','展开：准备确认、慢速沾色、暂停和部分画作导出')}<nav><a href="r2-browser/prepared/original-studio.png">保留原版的主画室</a><a href="r2-browser/prepared/checks.json">准备与接触检查</a></nav></section>${cards}<section><h2>重播、降级与连续运行</h2><p>三张完整计划分别在 0.5×、1×、4× 测试时钟下执行并校验颜色与高度；这是调度一致性测试，不是实时录像。下面的真实页面录像验证首阶段重播、1× 起笔后切换 4×、半笔暂停、导出、降级续画与原作品保留。完整实际三图录像见上方。长录像录于工具栏启动性能补正前，落笔计划和结果保持相同；本节与顶部是补正后的准备/操作录像。</p>${video('r2-browser/controls/process.webm','展开：实际首阶段重播、半笔暂停与降级续画')}<nav><a href="r2-browser/controls/results.json">速度与隔离结果</a><a href="c-engine/tests-results.json">完整计划三种速度检查</a><a href="performance-r2/continuous.json">独立性能原始数据</a></nav><p>独立复杂场景 ${perf.speed}×，未录屏、截图或导出；实际耗时 ${(perf.wallMs/60000).toFixed(2)} 分钟。批次 p95 ${perf.batches.p95.toFixed(2)} ms，播放帧间隔 p95 ${perf.playbackFrames.p95.toFixed(2)} ms。播放期记录 ${perf.playbackFrames.over100ms} 次超过 100 ms 的间隔，最大 ${perf.playbackFrames.max.toFixed(0)} ms，原因尚未确认，不能称为零卡顿。帧间隔不是输入延迟，页面堆内存不代表 GPU、Worker 和原生缓冲总内存。</p></section><footer>所有原图沿用有权再分发的固定样本；不上传或提交私人图片。本轮只推送 E1 分支，线上仍为 M2。真实触控笔、低配置设备和人工观感未测试；实验退出前请导出。</footer></main></body></html>`;
for(const m of html.matchAll(/(?:href|src)="([^"]+)"/g)) if(!existsSync(resolve(root,m[1]))) throw new Error(`Missing evidence: ${m[1]}`);
writeFileSync(`${root}/index.html`,html);
console.log('Prepared studio gallery generated; every local target exists.');
