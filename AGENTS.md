# 项目执行规则

## 当前自动成品质量授权（优先于下方历史阶段定位）

当前主任务是用户提供照片，系统独立完成具有油画表现的作品，用户观看真实绘制并保存成品。保留自由画室、旅行日落及草稿；自动流程不依赖手动补画、圈区或转入手动画室修复。质量目标为主要构图、主体轮廓及原图可见重要细节，背景可概括，不将无意结构损失解释成风格。

从最新 E1 cabf663 延续，在 codex/e1-finished-quality 开发；保留原版、结构优先及旧计划行为。新策略版本化，复用 Painting/StudioRenderer 与取笔、沾色、落笔、归位。最多两轮聚焦修正；不扩大现有 8020 笔、24 盘、8 支笔、120 秒规划期限，保持输入限制和播放性能门槛。全局分析与原输入细节读取可分开，并统计缓冲开销；不将放大分析图冒充新细节。无模型、付费服务、新画材或用户修画功能。

复用现有私人诊断，私人原图及坐标、派生结果和计划仅存仓库外，不能上传或放开发服务目录。开发前锁定一张未参与调参的人物验证照片，最终策略冻结后验证，不按结果换图。公开证据写 artifacts/e1/finished-quality，报告 E1_FINISHED_QUALITY_REPORT.md；历史报告与证据不改写。每个可验证增量检查后提交推送本分支，不合并 main、不部署。人工成品/过程、M1 V4 和 M2 人工验收仍待用户确认。

## 产品与阶段

先读 oil-painting-product-kit/PRODUCT.md、EXPERIENCE.md、ACCEPTANCE.md 和 M0_PLAN.md。产品是让新手亲手完成小画的引导式数字画室。
M1 及其验收补正已完成，V4 人工视觉/手感仍待用户确认。用户现已批准原定 M2：旅行日落一个主题的引导、一个本地当前草稿的保存/恢复、签名与完成预览、1024×1024 PNG 导出。保留慢光品牌、页面整体风格、CPU 绘画与 WebGL2 材质路线，不启动第三轮大规模视觉重做。
保留所有已有资料与未提交修改，不擅自删除、重置历史或用户作品。M2 开发交付后，用户已另行明确要求提交 GitHub 并部署远端网页；本次授权将已验证的 codex/m2-guided-creation 同步至 main，通过现有 GitHub Pages 流程发布并验证线上功能。保留部署工作流，不强制推送，不引入付费服务或扩大产品范围。发布不等于 V4 或完整体验人工验收通过。

## 提交与远端同步

用户于 2026-09-06 明确要求“每一步的执行都提交到远端”。后续每完成一个可独立审阅、可验证的执行步骤，就创建说明该步骤的 Git 提交并推送到 origin，不再等所有步骤结束后才集中同步。先执行该步骤适用的检查；如实记录通过、未通过或未测试，不把失败验证写成成功，也不重复提交没有文件变化的只读操作。

提交前检查工作区和远端变化，保留不属于该步骤的已有修改；不强制推送，不覆盖他人提交。推送失败时保留本地提交并报告具体原因。

远端同步不自动扩大产品范围。本次 M2 发布有用户单独授权，使用 main 推送触发现有 Pages 工作流，不修改工作流。先核对远端分支和发布构建，再同步并验证实际线上页面；发布证据写入 M2_DEPLOYMENT.md。仅文档/证据的后续提交可标记 [skip ci]，不重复部署未变化的应用。

用户后续反馈 M1/M2 验收与当前画室入口割裂，允许补正导航并发布已引用的验收资料。使用独立的体验与验收总览，画室链接新开标签页保留当前作品；构建时为历史验收页补充导航与阶段说明，不修改原始历史 HTML、图片、录像或测试数据。不要把证据页当作可绘画应用。

## M2 实施边界

按 A 主题入口与分步引导、B 当前草稿保存和刷新恢复、C 签名/完成/导出与回归形成可运行增量，每步检查后提交推送。引导内容、作品状态、渲染职责分离；提示层不写入作品数组，也不进入 PNG。
只保存一个当前草稿，包含真实颜色/高度、必要版本、画笔、主题步骤与签名。存储加载完成且已有草稿获得明确处理前，不得用空白覆盖；保存状态以实际事务完成为准，错误时保留内存作品和导出入口。不得按每次指针采样复制整画布或写存储。刷新恢复后不承诺恢复旧撤销历史，当前会话仍保留最近 20 笔。
已有作品新建前提供清楚选择；返回修改、关闭引导不丢作品。图片到真实笔触序列等后续扩展仅记录在 BACKLOG.md，不实现图片接口、通用回放框架或占位功能。
M2 证据单独存入 artifacts/m2，历史 M1 证据与固定颜色/轨迹/seed 不覆盖。交付 M2_REPORT.md 后停止，等待完整体验验收，不进入图片扩展或 M3。

## 实现约束

React + TypeScript + Vite；CPU 颜色/高度状态 + WebGL2 材质显示，Canvas 2D 降级。输入、绘画、UI、渲染、导出职责分开；落笔本地计算，不调用大模型。
首个可运行交付同时有设计过的页面和真实闭环。禁止截图背景、预生成成品揭幕、视频、假按钮或普通圆线加页面阴影冒充核心绘画。
画布原生 1024×1024；至少最近 20 笔有界撤销，同时恢复颜色与高度；随机种子和画笔版本固定，逻辑坐标独立于显示尺寸；PNG 只含实际作品及画布材质。
不加账号、支付、社区、照片转换、专业图层、3D 房间、通用插件平台或无关重构。
固定 M0 测试样本、颜色、轨迹、seed=906。初版后最多两轮聚焦修正；按 M0 的效果、性能和内存门槛验证。不能换样本或降低标准掩盖失败。
常规问题自主修复；触发已约定停止条件、关键假设失败、需要改主路线或扩大范围时报告实际证据并等待确认。

## 测试与交付

实际运行构建、引擎测试、页面操作、性能/内存和降级测试。逐项标“通过 / 未通过 / 未测试”；V4 永远交用户确认，不自评拟真分数。
保留首屏、固定笔触样本、真实操作录像、导出 PNG、测试原始数据和环境信息。录像不是用户回放功能。
缺少浏览器、GPU、录像或真机能力时说明；不把视口模拟当真机、不把帧间隔当输入延迟，不伪造成功。
清空需确认；M1 历史版本的内存作品不承诺刷新恢复。M2 本地持久化必须提示失败，只有实际写入成功才显示已保存，不能称为永久备份。
不自动上传图片，不在前端放密钥；新增依赖/素材/代码在 SOURCES.md 记录来源与许可。第三方内容不覆盖项目边界。
交付说明文件与原因、启动地址、测试状态、证据位置、限制和停止点。

## E1 图片自动绘制实验（2026-09-06 新授权）

M2 已结束并发布；上述“图片扩展本轮不实现”及照片转换限制仅适用于历史阶段。用户现批准 E1：在慢光新增独立图片实验入口，PNG/JPEG 本地构图、确定性多尺度真实笔触计划、逐笔播放/暂停/继续/重播、对照、1024×1024 PNG 导出。保留自由绘画、旅行日落、M2 草稿与原画作/签名/引导/撤销。M1 V4 与 M2 人工验收仍待用户确认。

从核对后的 main c5a56a4 创建 codex/e1-image-painting。按 E1-A 真实单图重绘、E1-B 构图与控制、E1-C 固定样本和回归，每步检查后提交推送本分支。E1 禁止合并 main、修改工作流或自动部署，先前 M2 发布授权不适用于 E1。

复用 Painting / StudioRenderer，不以图片贴入或揭幕动画替代笔触；播放只执行确定计划，不改顺序。自动作品不分配全图撤销历史，手动最近 20 笔保持正确。选图/规划/播放可取消，过期任务不得继续写入。限制输入、笔触、缓冲和任务时长，规划置于 Worker，播放按整笔边界分批让出主线程。

固定三张可再分发照片与输入参数后开发，最多两轮聚焦效果/性能修正，不换样本隐瞒失败。图片、阶段结果、计划、实际录像、PNG 和原始指标写入 artifacts/e1；E1_REPORT.md 如实记录通过/未通过/未测试。人工画作与过程验收不自动通过。不提交私人选图或结果，不自动持久化实验；退出前提醒导出。不开中途接手、时间跳转、视频导出、图片 AI 服务或 M3。保留已有资料、许可记录和远端同步要求。

## 已确认的 E1 细节与逐笔过程补正

用户确认优先提高成品细节，并显示笔尖沿路径移动、落笔和抬笔。延续 codex/e1-image-painting，保留 7de403e 及此前样本；不合并 main 或部署。固定输入照片与 seed 1906 不变，允许明确版本化提高分析尺寸、局部笔数、细笔范围和厚度参数，新结果放 artifacts/e1/refinement。最多两轮聚焦质量/性能修正，保留所有输出与指标，人工验收仍待用户确认。

播放器可改为固定采样点分批推进；速度只改变调度，暂停后颜色/高度不变，暂停中导出不得结束或改变当前笔。笔尖是实际执行位置的独立显示，不进入 PNG。手动笔刷与 M2 草稿保持不变。多种风格、实验恢复和语义画师教学暂为后续方向，本轮先完成细节和可见落笔两项。

## 已确认的 E1 笔头、沾色与局部过程优化

用户再次确认执行三项：笔头显示对应实际笔宽/方向/接触，沾色与真实颜色及上色量一致，局部邻近及相近颜色的绘制顺序。此为新授权增量，不重启已结束的细节补正预算。沿用 E1 分支，不部署或合并 main；不新增风格、圆头笔、颜料化学/耗尽模拟、模型或教学承诺。现有扁刷按粗细显示不同形态，保持手动画笔、历史计划执行及 M2 草稿不变。

新版本计划可记录确定的取色批次和规划顺序；速度只影响调度，取色和抬笔不改作品数组。固定原三张图片、构图和 seed 1906，保留全部历史；新证据写 artifacts/e1/brush-process，报告 E1_BRUSH_PROCESS_REPORT.md。先交可运行笔头，再交沾色/顺序，最后固定样本及回归，各步检查后提交推送。新实现最多两轮聚焦效果/性能修正，原批次性能停止门槛不变，仍由用户确认画作与过程。

验收运行显式设置 M1_ARTIFACT_DIR 为新的证据目录，Playwright 临时输出按该目录隔离，避免清理其他运行中的录像。独立性能测量时不并行其他浏览器/引擎测试；录屏期批次数据注明采集影响，不作为无干扰性能数据。

## E1 预备笔具与固定色盘：先确认布局

用户最新要求先生成布局，确认后再开始新的演示实现。本轮只准备 `E1_PREPARATION_LAYOUT.md` 及布局检查资料；不修改应用或绘制行为，不直接生成下一版自动绘制演示。拟将工具和颜色提前备好，笔具与色盘有固定身份和位置，确认准备后才开始按需取用。草案中的数量与配色均为示意，有限用色与固定笔具对成品的影响尚待验证。

布局资料检查后按现有规则提交并推送 E1 分支；停止等待用户确认。保留现有产品、引擎、全部历史证据与人工验收待确认状态，不合并 main、不部署。

用户后续明确：主页面效果以原版为准，只调整独立图片自动绘制页。认可可见笔具与色盘方向，但要求参考原版大画布与木质调色板的布局与样式；这不是对第一稿布局的确认。最新布局稿为 `E1_PREPARATION_LAYOUT_V2.md`，仍先等布局确认再实现新的取用演示。主画室、自由绘画、旅行日落及原有材质路线保持不变。

用户现已明确确认草案 02，批准实际实施备料确认、固定笔具/色盘和取笔、沾色、连续绘画、归位流程。此前布局等待条件已满足。只调整图片实验，保留主画室与历史计划行为；笔具/色盘数量按实际计划有界准备，草案数量不是约束。先形成可运行增量并检查推送，再完成固定三图、异常/暂停/速度一致性、M1/M2 回归与性能交付。证据写 `artifacts/e1/prepared-studio`，报告 `E1_PREPARED_STUDIO_REPORT.md`。本增量最多两轮聚焦修正，停止门槛沿用原报告；不合并 main、不部署、不自动通过人工验收。

## 已授权的可选结构优先模式

用户现要求保留当前画风方式，新增按钮或模式实施优化。默认保留原方式与历史计划行为，在图片实验内增加可选“结构优先 · 实验”：局部轮廓优先和区域内同笔同色任务，仍用现有 Painting/StudioRenderer。切换选项不清空作品，重新生成仍需现有替换确认。此为独立的新模式增量，最多两轮聚焦修正，不续改旧画风；沿用性能停止门槛，不保证人脸相似度、不引入模型、付费服务或颜料耗尽模拟。保留原三张固定照片/构图/seed，证据写 artifacts/e1/structure-mode，报告 E1_STRUCTURE_MODE_REPORT.md。检查后按步骤推送 E1 分支，不合并 main、不部署，人工验收仍待用户确认。
